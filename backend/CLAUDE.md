# CLAUDE.md — Tennis Prediction & Betting Engine

> Last updated: 2026-08-06 (Nodo-174: Auditoría Conexiones Rotas + Lazo de Validación — 14 deliverables D174-01→D174-14, todos COMPLETO. Hallazgo raíz: el sistema no aprendía porque `n_actual` de las hipótesis no lo escribía nadie y 2 símbolos marcados ✅ en CLAUDE.md (`_calc_hcuc_convergence`, `outcome_id`) nunca habían sido commiteados — más `pytest tests/` abortando en colección. D174-01 repara la colección + implementa de verdad `_calc_hcuc_convergence()` (edge_calculator.py:1003). D174-02 baseline honesto + triaje obligatorio de cada fallo (prohibido "N pre-existentes sin relación" sin enumerar). D174-03 `core/hypothesis_ledger.py` — PASO 10e escribe `n_actual` real por primera vez. D174-04/05 predicados + pre-registro de las 18+4 hipótesis huérfanas sin ruta de medición. D174-06 `live_desk.py` deja de mostrar `n_actual` inventado. D174-07 segmenta `weather_flag`/IRP en shadow_book. D174-08 implementa `outcome_id` real (edge_calculator.py:1462), el otro símbolo fantasma. D174-09 corre `audit_phantom_history.py` retroactivo. D174-10 tests para 3 estrategias sin cobertura. D174-11 telemetría del gate Kambi. D174-12 resuelve módulos huérfanos (retira/reclasifica, sin código nuevo). D174-13 `check_contradictions.py` Bloque E — `_check_simbolos_verificables()`, ningún ✅ futuro sin evidencia en código (0 símbolos sin evidencia verificado contra el repo real). D174-14 rectifica CLAUDE.md: cerrado el único fallo real del suite completo (`test_nodo42.py::test_t42_07_superficie_filter_excluye_clay`, categoría (i) triaje D174-02 — test aislaba mal `_load_edge_report_index()`, colisionaba con un jugador real de producción, no el gate EV_LEG_MIN que el comentario viejo citaba) y reconciliadas (no marcadas PENDIENTE) las entradas Nodo-155 D155-02/Nodo-154 D154-06 porque D174-01/D174-08 de este mismo Nodo las implementaron realmente. Baseline final verificado: **2699 passed, 0 failed, 3 skipped**. Spec: [[Nodo-174-Auditoria-Conexiones-Rotas-Lazo-Validacion.md]].) (Nodo-173: Calibración p_modelo + Embudo Decisión — BLOQUE A/B/E IMPLEMENTADO, BLOQUE C cerrado legítimamente (PUERTA 3: skill≤0). 34 tests REGLA-T53 + 3 regresiones pre-existentes arregladas.) (Nodo-172: Ancla Segura — Filtro `confidence_flag` en Combos Multi-Pierna (D172-01/02/03). Auditoría 2026-08-03 combo real Castellanos+Mejia+Udvardy+Ruse @49.33x ($500→$24.6k) reveló: 1 STRONG + 3 LOW confidence_flag, ganó por varianza no señal. Root cause: `build_live_combos()` ni `build_system_combos()` leían `confidence_flag` para filtrar fillers de calidad — aceptaban cualquier pick con cuota≥1.50 Kambi. D172-01 nueva estrategia `build_ancla_segura_combos()` en `betplay_combo_builder.py` — ancla SOLO confidence_flag=STRONG + cuota≥2.50, fillers SOLO STRONG/MODERATE (LOW excluido duro), sin relaxation si faltan fillers (fail-loud). D172-02 CLI flags `--ancla-segura`, `--ancla-segura-stake` (default $3k), `--ancla-segura-fillers` (default 3), `--ancla-segura-cuota-min` (default 2.50). D172-03 dispatch en `--live` flow combinado + standalone mode, guard D171-01 actualizado. D172-04 funciones soporte `_mostrar_ancla_segura()`, `_generar_bat_ancla_segura()`, `_enviar_ancla_segura_telegram()` + strategy tag ANCLA_SEGURA en ComboRegistry (D144-08 gap). 8 tests REGLA-T53 (8/8 PASS: test_172_01 caso exacto 2026-08-03 retorna [] no combo, test_172_02-08 fillers STRONG/MODERATE/gates). Suite: 2541 passed, 27 pre-existentes sin relación, 3 skipped, 0 regresiones. Spec: [[Nodo-172-Ancla-Segura-Filtro-Confidence-Flag.md]].) (Nodo-171: GAMES --live Fallback Silencioso a Reporte Estático Stale — usuario reportó primero "no llegó nada a Telegram toda la tarde" pese a D166-01 disparando en vivo; tras corregir eso, reportó "combos abren Betplay sin picks" — el mismo síntoma de [[Nodo-162]]/[[Nodo-169]], ahora en GAMES live. 3 bugs encontrados: D171-01 —`sys.exit(1)` cuando `build_live_combos()` vacío (trader_plans stale, normal por la tarde) mataba TODO el proceso antes de llegar a `--games`/`--mega`/`--safe`/`--sistema` (modos independientes), explicando "nada llegó a Telegram"; D171-02 — `",".join(outcome_ids)` asumía str pero Kambi IDs son int → TypeError silencioso en 5 sitios (D157-06/D170-01); D171-03 (el más grave) — `build_games_combos_live()` legítimamente retorna [] cuando no hay candidatos (gates D150/D151 funcionan bien), pero código caía a `build_games_combos()` reporte estático games_signal_report_*.json (generado 1 vez/día a las 08:39, nunca refrescado intraday) — enviaba combos con outcome_ids de partidos ya cerrados en Betplay (coupon abre vacío). Confirmación: log de mi propia prueba de verificación mostraba `"📄 Leyendo: games_signal_report_20260804_083949.json"` (exacta línea de `build_games_combos()`), muy probablemente generó el mensaje "GamesA/GamesB" roto que el usuario reportó. Fixes: D171-01 mover sys.exit solo si ningún modo independiente solicitado; D171-02 coerción `str(oid)` en 5 sitios; D171-03 eliminado fallback a estático en `--live` (ahora o envía live-real o nada, nunca stale). 4 tests REGLA-T53 (test_171_01/02/03/04 — 4/4 PASS, invocan `main()` real con sys.argv mockeado). Suite completa 138 passed sin regresiones reales (3 fallas preexistentes confirmadas vía `git stash` baseline). Dry-run en caliente: log confirma skip sin fallback. Lección: tercera recurrencia de "abre Betplay sin picks" — todos fueron fallbacks silenciosos a datos stale, no problemas de formato de URL. Spec: [[Nodo-171-Games-Live-Fallback-Reporte-Estatico-Stale.md]].) (Nodo-168: Fix Steam Dedup-Guard + Línea/Cuota Actual Nunca Poblada en ITF — usuario reportó con evidencia real del dashboard ("STEAM: -, en toda las filas") que Nodo-167 no funcionaba en producción pese a estar declarado "completo" con 11/11 tests. Re-auditoría honesta encontró 2 bugs: Bug A (D168-01) — `_write_games_odds_history()` tenía un guard de deduplicación con `continue` que saltaba también el cálculo de steam (`velocity_zscore`) cuando la cuota no cambiaba respecto al último punto grabado — condición que ocurre en la mayoría de los ciclos de 15s, dejando steam sin poblar casi siempre. Fix: separar el guard (solo bloquea `append`/`changed`) del cálculo de steam (corre siempre que haya historial). Bug B (D168-02) — el loop de `itf_live_signals` en `_check_games_convergencia()` nunca asignaba `linea_actual`/`cuota_actual`/`oc_id_actual` al dict `best`; D158-01 solo resolvía esto para `alta_signals`. `_build_x3_games()` ya leía esas claves desde Nodo-167 (D167-01) pero siempre recibía `None` para ITF. Fix: 3 líneas reusando `market_linea`/`cuota_val`/`market.get(oc_k)` ya frescos del mismo `_fetch_live_games_all()` que el loop ya llamaba — sin fetch adicional. 6 tests REGLA-T53 (test_168_01 y test_168_05 fallaron confirmadamente antes del fix, validando que detectan el bug real). Suite completa 2517 passed (2511 baseline + 6), 29 pre-existentes sin relación, 0 regresiones. Verificación en caliente con evidencia real: `reports/games_live_20260804.json` leído en 2 ciclos ~22s aparte con cuota SIN cambios entre ciclos (la condición exacta de colisión que causaba Bug A) — `steam_z`/`linea_actual` se mantuvieron poblados en ambos ciclos, no se resetearon a `None`. HTML servido en :7780 confirmado vía curl: badge azul "STEAM STEAM" en la señal con `steam_confirmado=True`, badges grises "z=-1.4"/"z=4.8" en las otras dos con historial suficiente, columnas LínAct/CuotaAct pobladas coincidiendo exactamente con el JSON subyacente — cierra la cadena completa dato→propagación→render. Lección: "tests verdes" no es evidencia suficiente cuando el bug vive en la intersección de dos mecanismos (dedup + steam) que cada test unitario del nodo anterior ejercitó por separado, nunca juntos bajo la condición de colisión real. Spec: [[Nodo-168-Fix-Steam-Dedup-Guard-Y-Linea-Actual-ITF.md]].) (Nodo-167: Completar wiring Nodo-160 (MC condicional+steam+IC95% CLT) + cerrar fuga linea_actual/cuota_actual X3 — D167-01/02/03/04/05/06/07: _build_x3_games() propagación, _resolve_player_rankings(), _attach_mc_conditional(), SE/IC95% analítico (CLT), steam wiring, badges X3. 11 tests REGLA-T53 (11/11 PASS: test_nodo160_mc_wiring 4/4 + test_nodo160_steam_detector 2/2 + test_nodo160_x3_visibility_wiring 5/5). Suite: 2511 passed (+10 nuevo vs 2501 baseline), 29 pre-existentes fallando (0 regresiones D167). H160-02 pre-registrada. Spec: [[Nodo-167-Wiring-MC-Steam-CLT-Linea-Actual.md]].) (Nodo-166: convergencia_score por pierna en alta_signals, gate por pierna reemplaza disparo D133 agregado — usuario aprobó explícitamente "Sí, replicar completo" tras Nodo-165: construir para ATP/WTA/Challenger/ATP1000/ATP500 la misma arquitectura convergencia_score que ya tenía itf_live_signals (D142-02+D165-01), reemplazando el umbral agregado `en_vivo_count>=2` por un gate individual `convergencia_score>=3`. D166-01: reusa sin cambio `_convergencia_score_itf()` y `_convergencia_certeza_bonus()`; único componente nuevo `_get_ranking_gap_er(home,away,er_picks)` — sustituye el proxy games-range de ITF por ranking_favorito/ranking_rival reales de edge_report (alta_signals sí tiene mercado pre-partido, a diferencia de ITF qualy). `alta_pregame_raw` filtra por score>=3 + not cuota_envenenada + not tiebreak set1, `convergencia_activa=len(alta_pregame_raw)>=1`. Regresión detectada y corregida en test_nodo133_games_live.py (codificaba el disparo agregado viejo) — reescrito con datos de mercado reales + test de control negativo nuevo probando que el conteo solo ya no dispara. 12 tests REGLA-T53 (6 nuevos test_nodo166 + 6 test_nodo133 actualizados/nuevos) — 12/12 PASS. Suite completa 2501 passed, 39 failed pre-existentes sin relación (baseline idéntico, 0 regresiones). Verificado EN CALIENTE post-restart tennis-live-desk (PID 40111→49967, HTTP 200, journalctl limpio): 7 señales alta_signals EN_VIVO reales con convergencia_score calculado (Udvardy vs Lys score=3 sin cuota_envenenada → calificó; van Assche y Berrettini score=3 pero cuota_envenenada=True → excluidos correctamente por el gate D164). H166-01 pre-registrada. Spec: [[Nodo-166-Convergencia-Score-Alta-Signals-Gate-Por-Pierna.md]].) (Nodo-164: Gates D150 Tier-Agnóstico — usuario corrigió premisa mía previa: Challenger/ATP1000/ATP500 en fase previa/qualy tienen volumen similar a ITF, no escaso; pidió replicar (no reinventar) la ingeniería de riesgo ITF a esos tiers. Auditoría de código confirmó que D147 (certeza/zona) y D151 (`_edge_live_gate`/`_score_null_gate`/`_zona_direccion_gate`) YA eran tier-agnósticos — D147 duplicado línea por línea en `live_desk.py` para `alta_signals` (:4091-4142) e `itf_live_signals` (:4414-4437); D151 confirmado aplicado a `estado in (EN_VIVO, ITF_VIVO)` sin distinción de tier en `betplay_combo_builder.py::build_games_combos_live()` (el punto real donde se arma el coupon, no el disparo `subprocess.Popen` de `live_desk.py`). Brecha real, acotada a 2 checks D150 exclusivos de ITF: `cuota_envenenada` (drift>+15%) y rechazo por set1-tiebreak — ambos solo calculados/gateados para `itf_live_signals`. D164-01: `cuota_envenenada` calculado también para `alta_signals` (mismo umbral 15.0, mismo patrón de log). D164-02: `build_games_combos_live()` agrega los mismos 2 `continue` (cuota_envenenada, set1>=12) para `estado in (EN_VIVO, ITF_VIVO)` por igual — centraliza el gate donde ya convivían los otros 3. 6 tests REGLA-T53 (6/6 PASS). Spec: [[Nodo-164-Gates-D150-Tier-Agnostico.md]].) (Nodo-163: Auditoría "0 APOSTAR" Tier Gap + Superficie Hardcode + Games Bridge Crash (D163-01/02/03) — usuario preguntó por qué run_daily no generó trader_plan hoy (edge_report 17 watchlist, 0 apostar). Hallazgo #1: --tier default omitía atp1000/atp500 (13/17 picks nunca evaluados). Hallazgo #2: superficie hardcodeada grass/clay para atp1000/atp500 corrompe calibración prior (Montreal/Toronto son hard en agosto, no grass). Hallazgo #3: evaluar_games_bridge.py no desempaquetaba tupla de _seleccionar_señal_optima (Nodo-149 D149-02 cambió firma) → crash reproducido cada corrida. Fixes: D163-01 agregar atp1000/atp500 a --tier default; D163-02 derivar superficie dinámica por tier desde edge_report; D163-03 desempaquetar tupla (juegos_optimas, sets_optimas). 7 tests REGLA-T53 (7/7 PASS). Validación: atp1000/atp500 ahora se evalúan con superficie correcta (hard). Suite 2478 passed, 39 pre-existentes sin relación a cambios. Spec: [[Nodo-163-Auditoria-0-Apostar-Tier-Gap-Games-Bridge-Crash.md]]) (Nodo-162: Redirect Page Coupon Formato Roto — usuario reportó link Telegram "ABRIR GamesLive" abría Betplay sin piernas cargadas. Root cause: `docs/bp/index.html` (GitHub Pages, sirve REDIRECT_BASE para TODOS los combos: SAFE/GAMES/CORE) tenía commit `4ae668d` (2026-07-28) que cambió el JS de formato correcto comma-joined a sufijo `|ML/` inválido — viola REGLA-BAT-1 (INMUTABLE, §9). Betplay no parsea el coupon roto → abre app vacía. Bug activo ~5 días, afectaba TODOS los combos por Telegram, no solo GAMES live. D162-01: revertido a `combination|ID1,ID2,ID3||replace` con comentario anti-regresión citando REGLA-BAT-1 y el commit que lo rompió. 3 tests REGLA-T53 (regresión estática, lee el HTML real). DEPLOY PENDIENTE: fix committeado local, branch 213 commits adelante de origin/main — usuario decidió hacer push manualmente, no automático. Spec: [[Nodo-162-Redirect-Coupon-Formato-Roto.md]].) (Nodo-161: Fire Guard Unificación — auditoría de conexiones ocultas encontró el mismo patrón "guard de disparo-único" (lista de listas, cap 10/día) duplicado literalmente entre el fire de games_live (D133-04) y el de itf_live_games (D150/D157) en `live_desk.py`. D161-01: `core/fire_guard.py` nuevo — `should_fire(path, key, cap=10)`/`mark_fired(path, key)` puras, extraídas sin cambio de comportamiento (D133-04 sigue marcando disparado solo si el Popen tuvo éxito; D150/D157 sigue reescribiendo `.bat`/`.html` cada ciclo de 15s fuera del guard, que solo controla log+Telegram+shadow_book). Deliberadamente NO unificados: `certeza_fired_{fecha}.json` (D147-06, dict sin cap) y el guard de `scripts/live_edge_monitor.py` (comparte responsabilidad con TTL cleanup de `.bat`, D116-01). 8 tests REGLA-T53 nuevos + 67 tests de regresión (Nodo-133/147/150/151/157) — 75/75 PASS. Spec: [[Nodo-161-Fire-Guard-Unificacion.md]].) (Nodo-160: Puente Conocimiento Live Intelligence — D160-01 auditoría confirmó `_check_games_convergencia` y `detect_break_state` corriendo con datos frescos (sin fix de deployment necesario). D160-02: `core/monte_carlo_games.py::simular_total_juegos_condicionado()` — Monte Carlo ligero condicionado a servicio (hold Bernoulli por servidor, tiebreak ajustado por fuerza relativa) + `estimar_p_hold()` proxy ranking/superficie (spec §3.3.1, riesgo declarado: no es serve-stats real). Wireado como check final pre-disparo en `_attach_mc_conditional()` dentro de `_fire_itf_live_games_combo()` — mismo patrón que `validate_fillability` D159-04 (una vez, sobre señales ya gateadas, no en cada refresh de 15s). `_resolve_player_rankings()` desambigua ranking home/away vía j1/j2 del h2h_idx. REPORTE_SOLO — `mc_p_condicional`/`mc_media_total_juegos` anotados en la señal, nunca gate (H160-02). D160-03: steam detector (`velocity_zscore`, Nodo-71) wireado en games market live (H160-03 pre-registrada, comparte gate con H52-05). D160-04: `core/confidence_kelly.py::confidence_scaled_stake()` generaliza `rival_value_betslip.py::micro_kelly()` (H88-01) — `micro_kelly()` ahora delega, mismo resultado numérico (regression-safe), cap más alto post-graduación (n_obs≥n_stop). D160-05: `core/live_signal_bridge.py::reconciliar_senales_partido(partido_key, games_state, winner_state)` — puente GAMES↔GANADOR, evidencia independiente que se refuerza sin penalizar en divergencia (mismo patrón que score_directo/score_rival_value de Nodo-98). Clasifica CONVERGENCIA_FUERTE (games DOMINANTE+certeza+break_situation Y score_directo≥3 Y break_state en BREAK_POSIBLE/CONFIRMADO) / DIVERGENCIA (games dominante pero drift_pct>+5% contra el favorito — solo se loggea, no dispara nada) / NEUTRO. No abre apuesta de ganador en vivo por sí solo — booster de contexto/confianza para X2 (Nodo-111) y n_obs efectivo (D160-04). H160-01/H160-02 pre-registradas (H160-03 ya estaba). 24 tests REGLA-T53 nuevos (9 D160-02 + 4 wiring MC + 6 bridge + 5 confidence_kelly). Spec: [[Nodo-160-Puente-Conocimiento-Live-Intelligence.md]].) (Nodo-157: Contrarian OVER Signal en Ruta Cuota Envenenada. D157-01: `_calc_over_candidato()` closure reutilizable en ambas rutas (linea_envenenada + cuota_envenenada). Badge azul "OVER — TERCER SET" cuando cuota_envenenada=True + over_candidato=True. D157-02 (bug fix): outcome_ids mercados EN_VIVO expiran en segundos — guard anti-flood bloqueaba reescritura del .bat/.html dejando coupon muerto. Fix: `_fire_itf_live_games_combo()` se reescribe cada ciclo (15s) con ID fresco mientras señal siga EN_VIVO; guard solo controla log/notificación. Servicio tennis-live-desk reiniciado. 4 tests REGLA-T53. Spec: [[Nodo-157-Contrarian-Over-Cuota-Envenenada.md]]) (Nodo-156-B: Sistema Leave-One-Out Combos 5-7 Piernas. D156-B-01: `build_system_combos()` en `betplay_combo_builder.py` — N piernas → N combos de (N-1) cada uno (1 pierna perdida no arrasa todo). Anclas=top-by-cuota (≥1.65 default), fillers=ranked-by-p_modelo descending, sin hard thresholds. D156-B-01a (bug fix): anchor classification removida de candidate collection (stackeaba casi todo como ancla por cuotas altas en edge_report) → ahora selecciona dinámicamente. D156-B-02: CLI args `--sistema`, `--sistema-piernas` (5-7, default 6), `--sistema-stake` (default $3500), `--sistema-ancla-min`. D156-B-03: dispatch combinado (tras GAMES en `--live`) + standalone MODO SISTEMA (antes de EVALUAR_GAMES). D156-B-04: output `Sistema*.bat`/`sistema*.html` + Telegram summary + ComboRegistry. H156-B-01 pre-registrada (≥80% hit combinado, n_stop=15). Smoke test 2026-07-31: 6 combos de 6 picks (pool: Kalieva @3.45, Michelsen @2.85, Hara Friend @2.28, Thomson @1.74, Stevens @1.96, Bouzige @2.48) → retornos $32,975–$65,382 @ $600 stake c/u. 8 tests REGLA-T53 pendientes. Spec: [[Nodo-156-B-Sistema-Leave-One-Out.md]]) (Nodo-155: HCUC Pipeline Integration — H152-01 no podía acumular n diariamente porque `scripts/detect_hcuc_picks.py` usaba campos mockeados que no existen en el pipeline real. D155-01: `analyze_surface_specialization()` gana 2 contadores aditivos `top20_wins`/`campeonatos_expirados_count`. D155-02: `_calc_hcuc_convergence(resultado, _surf_fav, _surf_dog)` en `edge_calculator.py` (junto a `_calc_elo_dominance_axis`) — asigna `hcuc_convergence`/`hcuc_signals` al final de `calcular_edge_completo()`, 100% observacional (no toca edge/kelly_kl/apostar). D155-03: segmento H152-01 en `shadow_book.py` (texto plano + checklist SPRT + tracker estructurado de hipótesis). D155-04: `umbrales_congelados` de H152-01 corregidos a nombres de campo reales (valores numéricos sin cambio — no es p-hacking, acumulación prospectiva aún no arrancaba). D155-05: retiro de `scripts/detect_hcuc_picks.py`. Patrón reutilizado de H89-02/H98-01: el flag se calcula en PASO 3 (edge_calculator, corre para todo partido en `run_daily.py`), `shadow_book.log_picks()` ya lo persiste, `--report` ya lo segmenta — cero cron nuevo, cero paso manual. 11 tests REGLA-T53 — 11/11 PASS. Spec: [[Nodo-155-HCUC-Pipeline-Integration.md]]) (Nodo-153: Score Intelligence + D153-04 Break Parity Fix (2407 tests) — bug: fórmula vieja `no-servidor lidera=quiebre` ignoraba paridad de servicio (N par/impar determina quien sirvió primero). Fix: calcular expectativa por paridad, compara vs realidad. `►0:1 N=1` = hold (away sirvió G1, ganó hold), `►2:1 N=3` = quiebre (away sirvió G1 pero home lidera). D153-01: current_set_home/away desde statistics.sets. D153-02: serving desde homeServe. D153-03: game_score desde score.home/away. D153-04: break_situation por paridad. `_fmt_progreso()`: `7:6,1:6, ►2:2 [15:30] | 24j QUIEBRE`. Parser en `_parse_kambi_livedata_sets()` llena D153 desde endpoint /event/{id}/livedata.json. 14 tests REGLA-T53 — 14/14 PASS. ARQUITECTURA: _fast_score_refresh() thread nuevo (5s) actualiza solo livedata D153 en games_live_*.json, reduce latencia 30s→≤7s. TTL livedata=4s (< ciclo 5s), STATE_CACHE=6s. Flujo: Kambi ~1s → fast loop ~5s → browser ~5-7s. Spec: [[Nodo-153-Score-Intelligence-Break-Parity.md]] (Nodo-154: Pipeline Integrity — 6 bugs silenciosos que causaron 0 combos ATP. D154-01: watchlist cap 10→50 en edge_calculator.py:1575 (53 picks ocultos para todos los builders). D154-02: D152-05 Phantom tier gate usa campo tier del h2h record en vez de detectar_tier(torneo_nombre) — detectar_tier("Sao Paulo")="atp500" hacía que Vesantera ITF se colara. D154-03: _find_latest_h2h() elige por n_partidos desc en vez de sort alfabético (Playwright 36p ganaba a API 366p). D154-04: Kambi matching añade _PARTICLES (De/Van/Del/Von) + fallback unigram exact (2/13→≥8/13 matches). D154-05: games_signal_calculator en run_daily PASO 3.6 pasa --file con el mismo h2h que edge_calculator (ya tiene argparse L780). D154-06: outcome_id propagado desde h2h record al edge_report output dict. D154-08: Kambi refresh pre-PASO 4 en run_daily (cierra G4+O3+B3+B8). D154-09: Ledger MOTOR↔combos (Nodo-155 candidato). D154-10 (O4): cuota_favorito refresh usando call D154-08 (edge_calculator.py:1014 estático). D154-11 (G3/O5): select_best_json_file() mode='h2h' unifica 3 implementaciones de file selection. Cobertura completa: 10 bugs + 5 gaps + 5 oportunidades = 20 issues. 10 tests REGLA-T53. SPEC — pendiente implementación.) (Nodo-152: Phantom History Guard — thf_cache asignó historial top-ATP a Vesantera B.T. (ITF M15): ELO=1974, edge=39.5%, AC1–AC12 apostados con datos falsos. D152-01: `_validate_circuit_consistency()` función pura 5 reglas acumulativas (R1 elite+100, R2 GS×top10+60 cap120, R3 median_opp<150+50, R4 thf_cache×1.5, contaminated≥50). D152-02: hard block en `_process_match()` ruta THF — historia limpiada, `match_data['_contamination_p1/p2']=True`. D152-03: `data_quality.history_contamination` en `_consolidate_result()`. D152-04: gate en `edge_calculator` leyendo history_contamination → status=NO_DATA. D152-05: ELO-ranking incoherence gate (elo>1800+ranking>400+tier=itf/challenger → segunda línea de defensa). D152-06: `scripts/audit_phantom_history.py` retroactivo 30 días. 8 tests REGLA-T53 — 8/8 PASS. 2371 tests.) (Nodo-151: Live Edge Gates — 3 gates corrigen root cause 1/5 hit rate simulación doctoral. D151-01: _edge_live_gate() — edge_live=p_condicional-(1/cuota_live)<5% → excluir (Modelo B sin poder de veto era el defecto central). D151-02: _score_null_gate() — score_str=null + games_played>3 → excluir (desventaja informacional: bookmaker tiene marcador real, sistema no). D151-03: _zona_direccion_gate() — zona contradice dirección + p_condicional<40% → excluir (caso Zarazua OVER@19.5 con DOMINANTE µ=18 = contradicción severa). 3 funciones puras en live_desk.py antes de _fire_itf_live_games_combo. Gates integrados en loop alta_itf después de D150-06b. H151-01 pre-registrada. 10 tests REGLA-T53 (6 base + 4 edge cases). 2363 tests.) (Nodo-150 addendum D150-07: CERTEZA_MATEMATICA override de LINEA_ENVENENADA — cuando certeza_matematica=True, badge no muestra LINEA ENVENENADA (elif añade condición `and not certeza_matematica`); combo gate alta_itf_raw permite paso OR certeza_matematica. Log [ITF_LIVE_D150-07]. H150-01/H150-02/H150-03 pre-registradas en preregistered_hypotheses.json. 3 tests nuevos REGLA-T53 (test_150_11/12/13). 13 tests totales Nodo-150. 2356 tests.) (Nodo-150: Live Games Risk Intelligence — 5 gaps post-simulación doctoral 2026-07-28. D150-01: CUOTA_ENVENENADA filter en _check_games_convergencia (umbral +15%, campo cuota_envenenada en señal ITF). D150-02: games_set1 en _parse_kambi_livedata_sets + propagación via _enrich_live_score. D150-03: zona=COINFLIP_FORZADO en call sites de _calcular_certeza_condicional cuando set1 tiebreak ≥12j. D150-04: parámetro games_set1 en _calcular_certeza_condicional — override interno DOMINANTE→COINFLIP. D150-05: badges MERCADO CONFIRMA (verde, drift<-15%) y CUOTA ENVENENADA (rojo) en render X3. D150-06: SET1_SCORE_GATE + CUOTA_ENVENENADA gate antes de _fire_itf_live_games_combo. 10 tests REGLA-T53. 2353 tests.)
> (Nodo-149: Separación definitiva mercados juegos/sets en games_signal_calculator.py. D149-01: mercado_tipo="JUEGOS"|"SETS" en las 4 señales de _analizar_mercados_juegos(). D149-02: _seleccionar_señal_optima() retorna tupla (juegos_optimas, sets_optimas) — nunca lista plana mixta. D149-03: señales_optimas=solo JUEGOS, señales_optimas_sets=SETS separado en resultado_base. D149-04: imprimir_reporte() combos A/B desde pool_juegos, Combo C desde pool_sets — pools nunca mezclados. D149-05: build_games_combos() en betplay filtra señales_optimas por mercado_tipo=="JUEGOS". D149-06: gap_sets=p_modelo_3sets-1/cuota con _P_3SETS_POR_ZONA={dominante:0.28,coinflip:0.60,ajustada:0.42}, threshold>=0.10. 10 tests REGLA-T53. 2337 tests.)
> (Nodo-148: Trader Plan Vacío — D148-01: trader_ev_tenis.py escribe plan vacío (individuales=[]) antes del return cuando 0 APOSTAR, desbloqueando SAFE/WAS/MEGA. build_live_combos detecta cobertura=[] → fallback a _build_live_combos_legacy(edge_report). Verificado 2026-07-28: 8 combos desde watchlist. 3 tests REGLA-T53. 2327 tests.)
> (Nodo-147: Live Score × Games Convergencia — Certeza Condicional en Tiempo Real. D147-01: _enrich_live_score() conecta _parse_kambi_tennis_score() al panel X3. D147-02: _calcular_certeza_condicional() modelo Gaussiano + tabla lookup — certeza_matematica/p_condicional/alerta_nivel. D147-03: _freeze_baseline_if_needed() congela cuota_t0/linea_t0 inmutable cuando señal pasa a EN_VIVO. D147-04: panel X3 nuevas columnas Base(T0)/Live/Drift/Progreso/Certeza con badges coloreados. D147-05: _write_games_odds_history() sparkline append-only cuotas games market. D147-06: banner CERTEZA_MATEMATICA + Telegram alert fire-once. H147-01 pre-registrada (DOMINANTE p_condicional>=0.70 games_played>linea/2, n_stop=20). 6 tests REGLA-T53. SPEC — pendiente implementación.)
> (Nodo-146: H2H_MODEL universe en favoritos_combo_builder — D146-01: _find_latest_h2h() + _leer_h2h_favoritos() leen h2h_results_enhanced directamente para picks cuota<1.50 descartados por REGLA-HF-1. Timing guard D145-02 integrado. MAX_H2H_MODEL_PER_COMBO=2. Automático sin flag. 9 tests REGLA-T53. 2324 tests.) Nodo-145: Pipeline Bugs tipo_cancha+timing — D145-01a: h2h_extractor (Playwright) tipo_cancha usa superficie como fallback + copia hora. D145-01b: ninja_h2h_parser (--api-mode) mismo fix en rama list de load_matches() + _consolidate_result() + hora propagada. D145-02: edge_calculator timing guard skip partidos >15min pasados. Root cause cascada 0 combos: tipo_cancha='N/A' → unknown bucket 24% hit → Kelly aplastado → pipeline_picks vacío → 0 CORE. 7 tests REGLA-T53. 2314 tests.) Nodo-144: Trazabilidad de Estrategia en Shadow Book — D144-01→D144-07. Campo 'strategy' top-level en _build_record(). tag_strategy(). log_pick() singular. segmento strategy en report(). backfill_strategy.py retroactivo (22 tags alta confianza, 1287 HISTORICO_SIN_TAG). 7 tests REGLA-T53. 2307 tests.)
> Spec-Driven Development (SDD). CLAUDE.md es VISTA DERIVADA — los nodos son la fuente de verdad.
> Leer completo antes de tocar código. Ver política de precedencia §10.

---

## 1. NORTE REAL

**Visión:** Hedge fund cuantitativo — cada partido = activo con vida útil 2-3h.
**Misión:** Apostar solo donde `P_modelo > P_implícita_bookmaker` con Kelly-KL.
**Métrica:** P&L positivo acumulado — NO accuracy.

| Meta | Métrica | Estado |
|---|---|---|
| ~~Datos limpios~~ | Fix scraper + surface_specialization | ✅ 2026-05-28 |
| ~~Accuracy > 55%~~ | Con superficie | ✅ 77.4% (n=31, clay) |
| ~~P&L positivo n≥30~~ | Kelly-KL + sesiones validadas | ✅ 2026-06-01 |
| **Escalar bankroll** | Edge validado, hedge fund activo | **EN CURSO** |

---

## 2. CONSTITUCIÓN — Reglas inmutables

1. **SDD:** Ningún código sin Nodo en `.spec/01_Nodos/`. Ver `PRE_IMPLEMENTATION_CHECKLIST.md`.
2. **GIT-FIRST:** Buscar en git history ANTES de implementar. `git log --all --oneline -- '*keyword*'`
3. **REGLA-T53:** Tests invocan función real del módulo — nunca hardcodean la fórmula.
4. **REGLA-HF-5:** KGR < 0 en output trader → NO DESPLEGAR. Sin excepción.
5. **REGLA-HF-1:** Cuota < 1.50 NUNCA en pool. KGR con heavy fav = -0.5085 (ruina).
6. **Playwright PRIMARIO:** PASOS 1+2. API solo si Playwright falla Y Phantom Guard activo.
7. **5 errores históricos:** Ver `docs/DECISION-LOG.md`. No repetir.
8. **Pre-registro:** Ninguna hipótesis sin H-XX en `validation/preregistered_hypotheses.json`.
9. **GRAPHIFY-FIRST:** Antes de implementar cualquier "pendiente" de un spec → `graphify query "<concepto>"`. Si aparece en el grafo → FALSO PENDIENTE, actualizar spec con L####. El hook graphify aplica ante LEER *y* ante IMPLEMENTAR. Costo de ignorar: 3 sesiones perdidas (Nodo-119, Nodo-123). Ver `PRE_IMPLEMENTATION_CHECKLIST.md §REGLA-GRAPHIFY-FIRST`.

---

## 3. FUNDAMENTOS CLAVE

```
Kelly-KL:   f*_KL = f*_clásico × exp(-λ × KL(P_modelo || P_histórica))
λ por tier: GS=1.0× | ATP1000=1.6× | ATP500=2.4× | Challenger=3.6× | ITF=4.5×
Portfolio:  factor = 1/(1+ρ×(N-1))  ρ: GS=0.25|ATP1=0.20|ATP5=0.15|CHA=0.10|ITF=0.05
VaR:        MAX_VAR_PCT=0.25 | g=E[log(1+R)] > 0 crecer | g<0 NO DESPLEGAR
CPPI:       cushion=(bankroll-FLOOR)/bankroll; factor=min(1,max(0,2×cushion)) — PROVISIONAL
GCS:        ACTIVO solo hierba. _GCS_BOOST_ENABLED=True, prior A60-01 (n=54, 64.8%)
Shrinkage:  n/(n+20). n=4→30%. n=33→62%. Lee calibracion_edge.json automáticamente.
```

Implementación: `analysis/rivalry_analyzer.py` | `edge_calculator.py` | `trader_ev_tenis.py`

---

## 4. FLUJO DEL PIPELINE

### ANTES DEL PARTIDO

```bash
# PASO 0 — Rankings (si están desactualizados)
python3 extraer_ranking_atp_version2.py && python3 extraer_ranking_wta_version2.py

# PASO 1 — Extraer partidos (PLAYWRIGHT PRIMARIO)
python3 extraer_URL_partidos_version2.py            # hoy  → data/zita_tennis_matches_YYYYMMDD_*.json
python3 extraer_URL_partidos_version2.py --tomorrow # mañana → nombre lleva fecha de mañana (D89-08)
# ⚠️  API fallback (SOLO si Playwright falla — Kambi ve menos partidos, 0 match_ids UTR):
# python3 extraer_partidos_api.py [--tomorrow] [--tier atp wta] [--torneo wimbledon]

# PASO 2 — Extraer H2H (API NINJA por defecto, Playwright si budget)
python3 extraer_historh2h.py --api-mode --all-tournaments             # hoy
python3 extraer_historh2h.py --api-mode --all-tournaments --tomorrow  # mañana (D89-08, auto-selecciona archivo)
# Playwright puro (lento, 2-3 min/partido): python3 extraer_historh2h.py --all-tournaments

# ── FLUJO NOCTURNO PROACTIVO (D89-08 — captura 34% más partidos) ──────────────
# ~22:00 CO: correr PASO 1+2 para MAÑANA → picks listos al amanecer (0 partidos perdidos por timing)
# python3 extraer_URL_partidos_version2.py --tomorrow
# python3 extraer_historh2h.py --api-mode --all-tournaments --tomorrow
# ~07:00 CO: correr PASO 3+4 con el H2H de anoche
# ──────────────────────────────────────────────────────────────────────────────

# PASO 3 — Edge Kelly-KL
python3 edge_calculator.py                          # → reports/edge_report_FECHA.json

# PASO 3.5 — Revisión humana (LEER ANTES DE APOSTAR)
python3 generar_tabla_favoritos2.py                 # → analisis_partidos_pandas.txt
# Revisar: contribution% | surface_specialization raw_score | Confianza <52% = señal débil

# PASO 3.6 (opcional) — Señales totales juegos/sets
python3 games_signal_calculator.py                  # → reports/games_signal_report_FECHA.json

# PASO 3.7 — Dual-Book Router X1 (Nodo-111, automático en run_daily)
python3 scraping/dual_book_client.py --compare --book2 data/zita_tennis_matches_*.json
# Imprime tabla: qué casa da mejor cuota por pick + ROI extra medio por routing

# PASO 4 — Deploy (UN tier por ejecución — --torneo-tipo FILTRA)
python3 trader_ev_tenis.py --bankroll 125000                              # GS clay (default)
python3 trader_ev_tenis.py --bankroll 125000 --superficie grass           # GS grass
python3 trader_ev_tenis.py --bankroll 50000  --torneo-tipo atp1000        # ATP1000
python3 trader_ev_tenis.py --bankroll 20000  --torneo-tipo challenger     # Challenger
python3 trader_ev_tenis.py --bankroll 10000  --torneo-tipo itf            # ITF
# Si KGR < 0 → NO DESPLEGAR. VaR auto-ajustado en "STAKES FINALES".

# PASO 4.3 — Combos de confianza (paralelo al pipeline)
python3 combo_confianza_builder.py --bankroll 125000 [--fase 1|2|3|4] [--anchor] [--telegram]

# PASO 4.4-4.57 (opcional) — Betplay + megas + safe
python3 betplay_combo_builder.py --live [--games] [--mega] [--safe] [--telegram]

# PASO 4.6 — Registrar apuesta
python3 betslip_registrar.py --listen               # antes de apostar (puerto 5001)
python3 betslip_registrar.py --cerrar               # post-partido → calibracion_edge.json auto

# ORQUESTADOR DIARIO
python3 run_daily.py [--bankroll N] [--tomorrow] [--settle-only] [--fase noche|manana|completa]
```

### DESPUÉS DEL PARTIDO

```bash
python3 shadow_book.py --close-snapshot             # PASO 5.5 — ~15 min ANTES del inicio
                                                    # AUTOMÁTICO: n8n (Nodo-73) via systemd tennis-snapshot-bridge
                                                    # FALLBACK: cron */10 con close_snapshot_trigger.py (si n8n cae)
python3 resultados_finales.py                       # PASO 6
python3 validar_con_api.py                          # PASO 7 → calibracion_edge.json
python3 consultar_resultados_historicos.py          # PASO 8
python3 pipeline_tracker.py [--section shadow|confianza|drift|portfolio]  # PASO 9 READ-ONLY
python3 shadow_book.py --settle YYYY-MM-DD          # PASO 10a
python3 shadow_book.py --report                     # PASO 10b — hit%, CLV, IC Wilson
```

### HERRAMIENTAS DE DIAGNÓSTICO

```bash
python3 pre_game_validator.py [--fixture]           # cron 0 9-23: BLOCK/WARN antes de apostar
python3 check_contradictions.py [--quick]           # cron lun 9am: CLAUDE.md vs nodos + FABLE §4.5 + frescura nodos_index (Bloques A/B/C)
python3 scripts/rebuild_nodos_index.py              # re-indexar tras añadir Nodo-*.md (Nodo-75)
/tennis-audit | /tennis-session | /tennis-brief     # slash-commands Claude Code
# Si Claude Code no responde → ver TROUBLESHOOTING.md
```

---

## 5. ESTADO ACTUAL — 2026-08-03

| Métrica | Valor |
|---|---|
| Tests | **2541 passed** (Nodo-172: +8 REGLA-T53 — 8/8 PASS; Nodo-171: +4; Nodo-170: +9; Nodo-168: +6, 2 fallaron confirmadamente pre-fix [test_168_01/05]; Nodo-167: +11; Nodo-166: +12; Nodo-165: +8; Nodo-164: +6). Suite completa: 27 failed + 3 skipped (todos pre-existentes, no causados por cambios recientes — verificado sin overlap con betplay_combo_builder/ancla_segura). Regressions: 0. |
| Calibración | clay GS: p=0.758 (n=31) \| global: wins=2358, losses=1480 (n=3838) \| ⚠️ buckets huérfanos `?`/`?_?` con ~141 resultados de dinero real (24% hit) — ver Nodo-86 §1.1, migración en evaluación: T7 Nodo-66 decide |
| **Auditoría Fable5** | **Sprints 1-5 EN CURSO.** S1-S4 ✅. S5: IRP ✅ (Nodo-96, 4361 perfiles, 15 tests). Pendiente S5: D90-11 N28F2/tier (gate n≥30), OddsAggregator multi-casa (gate cuentas reales) |
| Bankroll | $125,000+ |
| Shadow Book hit% | GS: 43.5% ROI+29.3% (n=23) \| Challenger: 38.4% ROI-3.1% (n=86) \| ITF: 41.1% ROI-9.3% (n=151) \| 302 settled / 71 abiertos (57 permanentes ITF) (jul-16) |
| ML Dataset | 2,573 registros limpios (motor nodo32, trazabilidad verificada) |
| Graphify | 949 nodos, 1,302 edges + 91 nodos .spec/ indexados (reindexado 2026-07-13 con Gemini). Tamp :7778 preset=aggressive, linger=yes. |
| **n8n** | **Docker :5678 + systemd tennis-snapshot-bridge :8765 — ACTIVO** |
| **GCS** | **_GCS_GATE_ENABLED=True — H60-01 GRADUADA 2026-07-10 (n=54, 64.8%)** |

**Fases FABLE_02:**

| Fase | Estado |
|---|---|
| F0 Reconciliación (C61/C62/C63) | ✅ completada |
| F1 Infraestructura (Graphify+Tamp+slash-cmds+validator) | ✅ completada |
| F2 Automation (n8n + close-snapshot timing exacto) | 🟠 PARCIAL — n8n+close-snapshot ✅ Nodo-73; C62-A código OK sin sesiones post-07-08 ⚠️; C63-B governor READ-ONLY 0 ejecuciones previas (gate: 10 sesiones) ⚠️; C63-A cola JSON implementada 0 activaciones ℹ️ |
| F3 Hermes gate | 🟠 GATED — observación ≥5 ambiguedades/semana |
| F4 Estadística doctoral (Nodos 64-71) | ✅ 67 tests (43 base + 24 C1/Nodo-67) |
| F5 Vault + session_compiler + CLAUDE.md slim | ✅ completada |

**Nodos completos:** 51-63, 64-71, 72, 73, 78, 86-113, 117-123, 126-129, 133, 135-141, 143-157, 156-B, 163, 164, 165, 166, 167, 168, 172, 174 (**165 nodos totales**) | IMPLEMENTADOS: [[Nodo-174]] (Auditoría Conexiones Rotas + Lazo de Validación — 14 deliverables, hallazgo raíz: el sistema no aprendía porque `n_actual` de hipótesis no lo escribía nadie + 2 símbolos ✅ en CLAUDE.md nunca commiteados (`_calc_hcuc_convergence`, `outcome_id`) + `pytest tests/` abortaba en colección; `core/hypothesis_ledger.py` nuevo escribe `n_actual` real por primera vez, `check_contradictions.py` Bloque E audita que todo ✅ tenga evidencia en código, baseline final 2699 passed/0 failed/3 skipped), [[Nodo-172]] (Ancla Segura — filtro confidence_flag en combos multi-pierna, auditoría 2026-08-03 combo Castellanos+Mejia+Udvardy+Ruse @49.33x identificó 1 STRONG + 3 LOW ganando por varianza; nueva estrategia #13 en betplay_combo_builder.py, ancla STRONG>=2.50 + fillers ONLY STRONG/MODERATE (LOW excluido duro, no relax si faltan), 8 tests REGLA-T53, guard D171-01 actualizado, strategy tag ANCLA_SEGURA en ComboRegistry D144-08), [[Nodo-168]] (fix steam dedup-guard bloqueaba velocity_zscore + ITF linea_actual/cuota_actual/oc_id_actual nunca poblados — encontrados por re-auditoría honesta de la afirmación "Nodo-167 completo" tras evidencia real del usuario, 6 tests REGLA-T53, verificado en caliente con JSON en 2 ciclos + HTML curl), [[Nodo-167]] (wiring Nodo-160 D160-02/03 — MC condicional+steam+IC95% CLT — D167-01/02/03/04/05/06/07, linea_actual/cuota_actual propagación, 11 tests REGLA-T53, H160-02 pre-registrada), [[Nodo-166]] (convergencia_score por pierna en alta_signals reemplaza disparo D133 agregado — reusa `_convergencia_score_itf`/`_convergencia_certeza_bonus`, nuevo `_get_ranking_gap_er()` — 12 tests REGLA-T53, H166-01 pre-registrada, verificado en caliente), [[Nodo-165]] (Bonus certeza D147 en convergencia_score ITF D165-01, +1 cuando alerta_nivel ALTA/CERTEZA y p_condicional>=0.85, cap=5, no toca gates D150/D151 — 8 tests REGLA-T53, H165-01 pre-registrada), [[Nodo-164]] (Gates D150 cuota_envenenada+set1-tiebreak tier-agnóstico D164-01/02 — 6 tests REGLA-T53), [[Nodo-163]] (Tier Gap + Superficie Hardcode + Games Bridge Crash D163-01/02/03 — 7 tests REGLA-T53), [[Nodo-157]] (Contrarian OVER Cuota Envenenada D157-01/02), [[Nodo-156-B]] (Sistema Leave-One-Out 5-7 piernas D156-B-01/02/03/04), [[Nodo-155]] (HCUC acumulación diaria H152-01), [[Nodo-154]] (B1-B10, G1-G5, O1-O5), [[Nodo-152]] (Phantom History), [[Nodo-150]] (Live Games) | detalles: `.spec/01_Nodos/Nodo-XX.md`

**Nodo-172 (Ancla Segura — Filtro confidence_flag en Combos Multi-Pierna — 2026-08-04):** Auditoría 2026-08-03 de combo real ganador ($24.6k) encontró raíz: Castellanos Y. (STRONG 0.801) + Mejia N./Udvardy P./Ruse G. (LOW 0.51-0.54) — 3 fillers near-coinflip acertaron por varianza, no señal reproducible. Investigación confirmó que `build_live_combos()` (D133-04) y `build_system_combos()` (D156-B-01) **nunca leían `confidence_flag`** del edge_report para aplicar criterio de calidad filler — solo aplicaban REGLA-HF-1 (cuota≥1.50) y disponibilidad Kambi. D172-01: nueva estrategia `build_ancla_segura_combos()` en `betplay_combo_builder.py` (~línea 3406-3610) — ancla SOLO `confidence_flag=STRONG` AND `cuota>=ancla_cuota_min` (default 2.50, nada de fallback a MODERATE), fillers SOLO `confidence_flag in ("STRONG","MODERATE")` (LOW **excluido duro**, nunca como fallback), ranked por p_modelo desc, top n_fillers (default 3). **Si no hay suficientes calificados → retorna ([], {}), no genera combo — fail-loud, sin relaxation.** Filosofía: no acepta señales débiles en fillers (diferencia crítica vs Nodo-156-B que no filtraba). D172-02: CLI flags `--ancla-segura`, `--ancla-segura-stake` (default $3000), `--ancla-segura-fillers` (default 3), `--ancla-segura-cuota-min` (default 2.50). D172-03: dispatch en `--live` flow combinado + standalone mode (antes de `--evaluar`), guard D171-01 actualizado para incluir `or args.ancla_segura` en condición sys.exit. D172-04: soporte functions `_mostrar_ancla_segura()`/`_generar_bat_ancla_segura()`/`_enviar_ancla_segura_telegram()` + strategy tag `ANCLA_SEGURA` en ComboRegistry.log_combo() (aborda D144-08 gap: nuevo strategy distinto para nueva estrategia). D172-05: coupon REGLA-BAT-1 compliant (comma-joined outcome_ids, `||replace`, sin `|ML/`), D171-02 style `str(oid)` coercion para int outcome_ids. 8 tests REGLA-T53 — 8/8 PASS: test_172_01 = caso exacto 2026-08-03 retorna [] (regresión guard), test_172_02 = 1 STRONG + 3 MODERATE/STRONG fillers → combo, test_172_03 = ancla siempre STRONG, test_172_04 = sin STRONG → [], test_172_05 = fillers insuficientes → [], test_172_06 = URL format REGLA-BAT-1, test_172_07 = REGLA-HF-1 <1.50 excluida, test_172_08 = STRONG <cuota_min no es ancla. Suite: 2525 total (2517 baseline + 8), 0 regresiones. Wikilinks: [[Nodo-156-B]] (Sistema Leave-One-Out, precedente sin confidence_flag filter), [[Nodo-165]]/[[Nodo-166]] (otras estrategias que filtran por calidad señal vía score/certeza), [[Nodo-144]] (strategy tagging D144-08), [[Nodo-150]]/[[Nodo-151]]/[[Nodo-164]] (gates de riesgo, mismo principio). Spec: [[Nodo-172-Ancla-Segura-Filtro-Confidence-Flag.md]].

**Nodo-165 (Bonus de Certeza D147 en convergencia_score ITF — 2026-08-03):** Usuario aprobó opción (b) de mi propia recomendación tras una investigación solicitada (solo análisis, sin código): señales ITF con certeza D147 fuerte real (p_condicional 0.94 y 0.962, alerta_nivel=ALTA, marcador en vivo confirmando la dirección) quedaban atascadas en `convergencia_score=1-2` — por debajo del umbral `>=3` que dispara el combo real (`live_desk.py:4500`) — porque `_convergencia_score_itf()` (D142-02, heurística gap/cuota/markov/ranking) es anterior a D147 y nunca lee su output. Casos reales auditados: Zheng vs Kecmanovic (gap+2, cuota_live=1.96<umbral 2.00, sin markov por falta de pick individual en edge_report para jugador ITF/qualy, score=2 pese a p_condicional=0.94) y Kovacevic vs Borges (mismo patrón, p_condicional=0.962, games_set1=13). D165-01: nueva función pura `_convergencia_certeza_bonus(score_actual, certeza)` en `live_desk.py` — bonus **+1** (cap=5, mismo máximo original) solo cuando `alerta_nivel in (ALTA, CERTEZA)` **y** `p_condicional>=0.85` (doble guard redundante pero defensivo); aditivo, nunca sustituye markov/cuota faltantes. Insertada dentro del loop de enriquecimiento D147 (`:4451-4494`), inmediatamente después de calcular `certeza` para cada señal ITF — es el primer punto del pipeline donde ese dato existe (el loop que llama `_convergencia_score_itf()` corre antes y ya cerró). Recalcula `confianza` para no dejar la etiqueta inconsistente con el score final; log `[ITF_LIVE_D165-01]` sigue el patrón de `[ITF_LIVE_D150-07]`. **Explícitamente NO tocado** (instrucción del usuario): `build_games_combos_live()`, `_fire_itf_live_games_combo()`, gates D150/D151 — verificado con test dedicado que el caso Kovacevic/Borges sube a score=3 con el bonus pero el gate de tiebreak downstream (`games_set1>=12`, lee el campo crudo, no `convergencia_score`) sigue excluyéndolo igual. Bonus aplica solo a `itf_live_signals`, no a `alta_signals` (disparo D133 usa criterio distinto sin gate de score por pierna). H165-01 pre-registrada (hit rate>=breakeven en señales disparadas exclusivamente por el bonus, n_stop=20, kill-switch ratio<0.80 con n>=15). 8 tests REGLA-T53 — 8/8 PASS. Wikilinks: [[Nodo-164]] (gates D150 tier-agnóstico, precedente inmediato), [[Nodo-151]] (gates edge_live/score_null/zona_direccion, no tocados), [[Nodo-150]] (cuota_envenenada+set1-tiebreak, gate independiente confirmado), [[Nodo-147]] (certeza condicional D147, fuente del bonus), [[Nodo-142]] (D142-02 convergencia_score original), [[Nodo-153]] (mismo patrón: dato de marcador real llega en loop de refresco separado). Spec: [[Nodo-165-Convergencia-Score-Certeza-Bonus.md]].

**Nodo-164 (Replicar Gates D150 Tier-Agnóstico — 2026-08-03):** Usuario corrigió premisa mía previa: Challenger/ATP1000/ATP500 en fase previa/qualy tienen volumen de partidos en vivo similar o solo levemente menor a ITF, no escaso — instruyó crear Nodo replicando (no reinventando) la ingeniería de riesgo ITF ya construida. Auditoría de código confirmó que la mayoría del stack YA es tier-agnóstica desde su implementación original: D147 (certeza condicional + zona con tiebreak-override) duplicado línea por línea para `alta_signals` (`live_desk.py:4091-4142`) e `itf_live_signals` (`:4414-4437`, comentario explícito "mismo flujo que alta_signals"); D151 (`_edge_live_gate`/`_score_null_gate`/`_zona_direccion_gate`) confirmado aplicado sin distinción de tier en `betplay_combo_builder.py::build_games_combos_live()` — el punto real donde se arma el coupon final, no el disparo `subprocess.Popen` de `live_desk.py`. Brecha real acotada a 2 checks D150 exclusivos de ITF: `cuota_envenenada` (drift>+15%, D150-01) y rechazo por set1-tiebreak (`games_set1>=12`, D150-06), ambos calculados/gateados solo dentro del loop de `itf_live_signals`. D164-01: `sig["cuota_envenenada"]` calculado también para `alta_signals` (mismo umbral 15.0, mismo patrón de log `[GAMES_LIVE] ... CUOTA_ENVENENADA`) en `live_desk.py`. D164-02: `build_games_combos_live()` agrega los mismos 2 `continue` (cuota_envenenada, set1-tiebreak) para `estado in (EN_VIVO, ITF_VIVO)` por igual — centraliza el gate en el único lugar donde ya convivían los otros 3 gates D151, sin tocar el bloque de disparo D-ITF-LIVE-02 de `live_desk.py` (sigue disparando/logueando/notificando ITF exactamente igual). No se replicó `over_candidato`/contrarian-OVER (D156-C) — estrategia de trading, no gate de seguridad, fuera de alcance. 6 tests REGLA-T53 — 6/6 PASS. Wikilinks: [[Nodo-150]] (origen cuota_envenenada+set1-tiebreak), [[Nodo-151]] (gates ya tier-agnósticos, confirmados sin cambio), [[Nodo-147]] (certeza/zona ya duplicados), [[Nodo-158]] (build_games_combos_live + patrón de test reusado), [[Nodo-133]] (disparo D133 alta_signals, no modificado), [[Nodo-163]] (mismo patrón de auditoría tier-gap, aplicado aquí al motor live en vez del pipeline de picks). CLAUDE.md §11 GAMES row (#11, ahora protección D150 uniforme todos los tiers), §2 regla 9 (SDD — Nodo documenta hallazgo antes de tocar código). Spec: [[Nodo-164-Gates-D150-Tier-Agnostico.md]].

**Nodo-163 (Auditoría "0 APOSTAR" Tier Gap + Superficie Hardcode + Games Bridge Crash — 2026-08-03):** Usuario reportó `run_daily.py` generó 0 APOSTAR aunque edge_report tenía 17 watchlist. Auditoría identificó 3 bugs silenciosos: D163-01 `--tier` default omitía atp1000/atp500 (13/17 picks = 76% nunca evaluados hoy); D163-02 superficie hardcodeada grass/clay para esos tiers corrompe `_load_p_prior()` calibración (Montreal/Toronto = hard, no grass); D163-03 `evaluar_games_bridge.py::272` no desempaquetaba tupla `(juegos, sets)` retornado por `_seleccionar_señal_optima()` desde Nodo-149 D149-02 → `AttributeError: 'list' object has no attribute 'get'` crash reproducido cada corrida. Corrección diagnóstica: `trader_ev_tenis.py --superficie` no filtra picks (falso previo), solo alimenta calibración prior. Fixes: D163-01 agregar atp1000/atp500 al `--tier` default en `run_daily.py:302`; D163-02 nueva función `_superficie_dominante_tier()` deriva superficie dinámica por tier desde edge_report actual (no hardcode); D163-03 desempaquetar tupla con `juegos_optimas, sets_optimas = _seleccionar_señal_optima(señales)` en `evaluar_games_bridge.py:272`. Validación en caliente: `trader_ev_tenis.py --torneo-tipo atp1000 --superficie hard` ahora evalúa 16 picks atp1000 (vs 0 antes); mismo para atp500. Confirmado: bloques correctos por T32-01 (p_modelo<0.55) y N28F2 (n_axes<2) — no son bugs nuevos. 7 tests REGLA-T53 — 7/7 PASS. Suite completa 2478 passed, 39 pre-existentes sin relación (grep: cero refs a run_daily/evaluar_games_bridge en ninguno de los 39). Wikilinks: [[Nodo-149]] (origen tupla), [[Nodo-154]] (patrón auditoría gaps PASO 3→4), [[Nodo-100]] (EL MOTOR por-tier), [[Nodo-72]] (validación identidades), [[Nodo-32]] (T32-01 gate), [[Nodo-28]] (N28F2 gate). CLAUDE.md §2 regla 2 (GIT-FIRST — clave para encontrar tier_config existente), §4 PASO 4 (now covers 5 tiers), §11 tabla estrategias (MOTOR row ahora con atp1000/atp500). Spec: [[Nodo-163-Auditoria-0-Apostar-Tier-Gap-Games-Bridge-Crash.md]].

**Nodo-156-B (Sistema Leave-One-Out — 2026-07-31):** Feature Request usuario: ampliar combos a 5-7 piernas siendo estratégico con anclas de cuota alta + asegurar que 1 pierna perdida no arrasa TODOS los combos. Solución: Leave-One-Out system bet — N piernas → N combos de (N-1) cada uno. D156-B-01: `build_system_combos()` en `betplay_combo_builder.py` — carga edge_report, filtra (cuota≥1.50 REGLA-HF-1, kambi_disponible≠False, no coinflip), selecciona anclajes (top-by-cuota ≥`ancla_cuota_min`, fallback top global) + fillers (ranked-by-p_modelo desc, sin hard threshold). D156-B-01a (BUG FIX): anchor classification removida de candidate collection — viejo código stackeaba casi TODO como ancla por cuotas altas en edge_report → ahora selecciona dinámicamente. D156-B-02: CLI args `--sistema`, `--sistema-piernas` (5-7, default 6), `--sistema-stake` (default $3500), `--sistema-ancla-min` (default 1.65). D156-B-03: dispatch combinado (tras GAMES en `--live`) + standalone MODO SISTEMA. D156-B-04: output `Sistema*.bat`/`sistema*.html` + Telegram + ComboRegistry. Smoke test 2026-07-31: 6 combos de 6 picks (Kalieva @3.45, Michelsen @2.85, Hara Friend @2.28, Thomson @1.74, Stevens @1.96, Bouzige @2.48) → retornos $32,975–$65,382. H156-B-01 pre-registrada (≥80% hit combinado, n_stop=15). 8 tests REGLA-T53 pendientes sesión futura. Spec: [[Nodo-156-B-Sistema-Leave-One-Out.md]].

**Nodo-154 — 20 issues auditados 2026-07-29 → IMPLEMENTADOS 2026-07-29 (11/11 tests PASS). SPEC: [[Nodo-154-Pipeline-Integrity-Watchlist-Phantom-H2H-Kambi.md]]. Tasks #34-#44 ✅ completadas.**

| ID | Tipo | Archivo:Línea | Deliverable | Estado |
|----|------|--------------|-------------|--------|
| B1 | Bug | `edge_calculator.py:1575` watchlist cap=10 (53 picks ocultos) | D154-01 | ✅ |
| B2 | Bug | `favoritos_combo_builder.py:611` _find_latest_h2h() sort alfabético | D154-03 | ✅ |
| B3 | Bug | `run_daily.py:372-376` kambi_disponible stale desde mañana | D154-08 | ✅ |
| B4 | Bug | `betplay_combo_builder.py:3225` partículas en nombres (2/13 matches) | D154-04 | ✅ |
| B5 | Bug | REFUTADO — 0 duplicados bidireccionales en h2h | — | ✅ |
| B6 | Bug | `ninja_h2h_parser.py:1742` hora parcialmente propagada | descartado | ✅ |
| B7 | Bug | `edge_calculator.py:1280` D152-05 usa detectar_tier("Sao Paulo")="atp500" | D154-02 | ✅ |
| B8 | Bug | `combo_confianza_builder.py:571` stale coverage (cascade B3) | D154-08 | ✅ |
| B9 | Bug | `run_daily.py:403` games_signal sin --file | D154-05 | ✅ |
| B10 | Bug | `edge_calculator.py:1014` cuota_favorito estática desde 08:32 | D154-10 | ✅ |
| G1 | Gap | `combo_confianza_builder.py:2058` ignora trader_plan (0 referencias) | D154-09/[[Nodo-155]] | 📋 |
| G2 | Gap | `edge_calculator.py:~1560` outcome_id ausente del output dict | D154-06 | ✅ |
| G3 | Gap | `file_utils.py:146` inteligente vs `favoritos_combo_builder.py:611` dumb | D154-11 | ✅ |
| G4 | Gap | `run_daily.py:423` sin Kambi refresh antes de PASO 4 | D154-08 | ✅ |
| G5 | Gap | `run_daily.py:403` games_signal universo distinto al edge_calculator | D154-05 | ✅ |
| O1 | Opt. | 63 picks edge>0 → solo 10 visibles en edge_report | D154-01 | ✅ |
| O2 | Opt. | MOTOR satura tier sin que combo_confianza lo sepa | D154-09/[[Nodo-155]] | 📋 |
| O3 | Opt. | `filter_kambi_picks.py:51` campo kambi_disponible stale | D154-08 | ✅ |
| O4 | Opt. | cuota_favorito refresh en mismo call Kambi de D154-08 | D154-10 | ✅ |
| O5 | Opt. | 3 implementaciones distintas de file selection → 1 en file_utils | D154-11 | ✅ |
**Nodo-142 (HUÉRFANO — sin spec):** ITF Live Games Convergencia (live_desk.py) — `test_nodo142_itf_live_games.py` existe sin spec. Deuda SDD: D143-03 crear spec en sesión futura.
**Nodo-64:** RFI Return-From-Inactivity — **implementado 2026-07-11 (D64-01)**: `rfi_tier`/`rfi_ultra`/`rfi_decay_gap` serializados en edge_report, segmentos en shadow_book --report. H76-01 acumula automático.
**Nodo-65:** Convergencia Multi-Señal — ANCHOR(edge>0) / VARIABLE(edge≤0). D65-01→D65-07. H77-01/02/03 pre-registradas.
**Nodos 66-68:** 66=checklist T1→T10 COMPLETOS. 67=integración herramientas **COMPLETO** (I3 governor JSON-first, I7 combo_registry→player_registry+run_daily settle, C1 DataContract v2 6 fronteras 24 tests, C4 brecha hit%_real vs shadow en dashboard). 68=H88-01 Rival Value Flip — **EVIDENCIA REAL 2026-07-14: 3/3 wins, combinada 41.25x** — n_actual=3, Wilson LB=0.526 > breakeven 0.267. D68-07 `rival_value_betslip.py` operativo (micro-Kelly shrink=5.7%, cap 0.5%). Gate: n=30 (faltan 27).
**Nodos 86-87 (auditoría):** 86=hallazgos. 87=12 fixes D87-01→D87-11+D64-01 aplicados, 18 tests REGLA-T53.
**Nodos 89-95 (Fable Sprint1→4):** 89=spec Sistema Inteligencia. 90=Auditoría Fable. 91=Sprint1 CAPA2+ELO_DOM+--fase. 92=evidencia ejecución. 93=Sprint2 PlayerDB+kambi. 94=Sprint3 PlayerIntelligence+PI en trader. 95=Sprint4 PatternRecognition REPORTE_SOLO (4 candidatos, confidence_flag=STRONG señal más robusta n=52).
**Nodo-96 (Sprint5-IRP):** IRP Individual Return-from-inactivity Profile — REPORTE_SOLO. `scripts/build_irp_profiles.py` → `data/irp_profiles.json` (4361 jugadores, delta_prom=-0.006). `irp_fav`/`irp_rival` serializados en edge_report. PASO 0c en run_daily. 17 tests (15 base + 2 H96-02 apellido fallback). H96-01 pre-registrada. Pendiente: D90-11 N28F2/tier (gate n≥30 CAPA2 settled), OddsAggregator multi-casa.
**Nodo-97 (Live Edge Monitor):** Spec completo — D97-01→D97-14 (incluye D97-13 shadow_book live + D97-14 Combo Governor). n8n PRIMARIO (cron=fallback). Ventana asimétrica [-30min,+45min] corregida. H97-01 pre-registrada (drift≥15%+edge>5%, n_stop=20). BLOCKER: Kambi LIVE endpoint pendiente verificación DevTools. Tests planificados: 8 en test_nodo97_live_edge.py.
**Nodo-98 (Meta-Señal Convergencia):** Spec corregido — score_directo (pro-fav, max=5) + score_rival_value (contrario, max=1) SEPARADOS. direccion=FAVORITO/RIVAL/SPLIT. Rival Value delega a rival_value_betslip.py (H88-01, no doble apuesta). PASO 3b asignado en run_daily. ELO dominance referenciado a Nodo-91. CLV pre-partido vs live separados. H98-01 pre-registrada (score≥3, n_stop=30). Tests: 8 en test_nodo98_meta_signal.py.
**Nodo-99 (Auditoría Fable N97+N98):** 3 blockers críticos + 5 gaps técnicos + 4 conexiones ocultas documentados. D99-01→D99-12. H97-01 pre-registrada. Specs N97/N98 corregidos. Triple Convergencia (C1: STRONG+rival_COLD+drift_live) = alpha oculto más puro. 100 nodos indexados.
**Nodo-100 (Triple Convergencia Live):** Break State Machine + Dashboard HTML + Auto-Combo. `detect_break_state()` 4 estados: NORMAL→BREAK_POSIBLE(drift≥15%)→BREAK_CONFIRMADO(2do ciclo ≥12%)→single-fire. `load/save_odds_history()` persiste por día. `_fire_break_combos()` llama betplay_combo_builder --live cuando break confirmado. `live_dashboard_generator.py` → `reports/live_dashboard.html` auto-refresh 60s con KPI boxes + tabla coloreada (gris/naranja/rojo parpadeante). `/live-dashboard` endpoint en close_snapshot_server. `--dashboard` flag en live_edge_monitor. D100-01→D100-07. H100-01 pre-registrada (n_stop=20, gate≥3 breaks). 5 tests REGLA-T53.
**Nodo-101 (Shadow Book Live CLV):** D99-02 implementado — `log_live_pick(pick, cuota_trigger, fecha)` registra picks live con `pick_type='live'` en sb_FECHA.jsonl (prefijo LIVE_ en sb_id). `settle()` ya usa `cuota_trigger` como base CLV para picks live (D101-03). `report()` muestra sección "LIVE PICKS H100-01" cuando hay settled live. CLI `--log-live JSON --trigger CUOTA`. `_fire_break_combos()` llama auto `log_live_pick()` tras BREAK_CONFIRMADO (D101-05). D101-01→D101-05. 4 tests REGLA-T53.
**Nodo-106 (Retroactive Settle Workflow):** Extensión T9 — workflow para cerrar picks abiertos cuando `--settle` retorna 0. Pasos: (1) extraer `h2h_file` de `session_meta` en sb_FECHA.jsonl, (2) `validar_con_api.py --no-cal` con ese H2H, (3) inyección programática `settle(fecha, resultados_map=...)`. Para GS/ATP sin cobertura API: WebSearch directo (no delegar al usuario). Ejecución 2026-07-16: +60 picks settled (242→302), 71 abiertos (57 permanentes ITF minors). Wikilinks: [[Nodo-66]] T9-ext addendum | [[Nodo-52]] | [[Nodo-81]] | [[Nodo-36]].
**Nodos 107-111 (Fable Sprint 5B — implementados 2026-07-17):** 107=MOTOR_DEFENSIVE x0.5 + governor soft-veto (D107-04) exit-codes 0/1/2 en 3 builders + H107-01 ACUMULANDO. 108=B108-01(rename Nodo-100B) + B108-03(name-matching→player_registry, 3 call-sites) + B108-04(checklist H89-*/shadow_book) + B108-05(curl OddsAggregator cerrado) + C3(campeon_signal estructurado) + B108-06(weather MVP). 109=live_desk.py :7780 7 paneles P4-MANDA + 3 funciones puras + 9 tests. 110=favoritos_combo_builder.py estrategia #13 + LEG_MIN_CUOTA=1.15 (D110-01) + H110-01(n=8, ACUMULANDO). 111=dual_book_client.py funciones puras X1(best_price/divergencia/es_arb/es_middle) + 14 tests + CLI --compare + PASO 3.7 en run_daily + H111-01 ACUMULANDO.
**Nodo-112 (C3 campeon_signal):** campos estructurados campeon_tier/torneo/days_ago en rivalry_analyzer — consumidores leen campo, no parsean strings de reasoning.
**Nodo-113 (B108-06 Weather MVP):** core/weather_client.py → get_weather_flag() open-meteo gratuito, weather_flag en edge_report (observacional), H113-01 pre-registrada (n_stop=40).
**Nodo-117 (Auditoría Scraping):** 4 bugs identificados 2026-07-18. D117-01 ✅: `_leer_matches_ranking_only()` prefiere ATP/WTA reales sobre CA/CB FlashScore (0→6 candidatos RANKING_ONLY). D117-02 ✅: `select_best_json_file()` prioriza `matches_with_cuotas>0`. D117-04 ✅: comentario anti-regresión `scraping/kambi_tennis.py` L196-199. D117-03 → implementado como Nodo-118. 9 tests REGLA-T53.
**Nodo-118 (Match Ledger Crosswalk — D117-03):** F1-F5 COMPLETOS 2026-07-18. `scraping/match_ledger.py`: `fusionar_dia()` Fellegi-Sunter simplificado (score 0-100, ≥75=AUTO-JOIN, 55-74=CUARENTENA, <55=single-source) + shortcut `match_id` compartido=score 100. `core/player_registry.py`: `add_alias()` + `resolve_crosswalk()` (MANUAL>VERIFIED>AUTO). `scripts/build_crosswalk_bootstrap.py`: 2091 identidades, 57.7% cobertura (194 zita + 152 edge_reports). `run_daily.py`: PASO 1b (Playwright) + PASO 1.5 (`match_ledger --build`). `live_desk.py`: panel DATA embudo fuga nominal. 35 tests REGLA-T53. Crosswalk en `data/player_crosswalk.json`. Gate F5 en acumulación: join ≥85% en ≥5/7 días.
**Nodo-119 (Auditoría Doctoral Desk v3):** Audit físico curl :7780 → 42 PASS / 21 FAIL. 11 fixes D119-01→D119-08 implementados en `live_desk.py` (render-only, REPORTE_SOLO). **Bugs estructurales críticos:** (1) P3 SIEMPRE VACÍO — `data.get("picks")` vs `apostar+watchlist`; (2) P6 NUNCA PARSEABA — regex single-line vs output multi-línea shadow_book. **Hallazgo operativo:** MOTOR cuota≤2.5 hit=48.2% vs cuota>2.5 hit=26.7% (gap 21.5pp — justifica H107-01). P6 ahora muestra 16 segmentos reales. 10 gaps pendientes: Tasks #48/49/50/54/59/62/63/64/65/66.
**Nodo-120 (FS Single-Source Cuotas — F6 Nodo-118):** Compuerta qualifying abierta 2026-07-19. `exportar_para_edge_calculator()` en `scraping/match_ledger.py` ahora incluye `single_source_fs` con `cuota1>0` (qualifying rounds con cuotas FlashScore, fuera del catálogo Kambi/Betplay). Campo `_cuota_source='flashscore'` para trazabilidad. Impacto: ~67 → ~100 partidos/día exportados (+33 qualifying). NO_DATA 51.9% hit rate ahora llega al edge_calculator. H120-01 pre-registrada (n_stop=20). 3 tests REGLA-T53.
**Nodo-121 (OddsAggregator Cuota Enrichment — F7 Nodo-118):** Hallazgo 2026-07-20: ss_fs con cuota1=None (qualifying no en PASO 1) SÍ están en betplay+rushbet via odds_aggregator (474 outcomes vs 122 de PASO 1). `enriquecer_ss_fs_con_aggregator()` en `scraping/match_ledger.py`: post-fusión, enriquece ss_fs sin cuota usando `fetch_all_odds(['betplay','rushbet'])` con match por apellido. CLI: `--build --enrich`. Resultado primer día: 5/11 enriquecidos (Aksu/Costoulas betplay=3.0, etc). `run_daily.py` PASO 1.5 actualizado con `--enrich`. H121-01 pre-registrada (n_stop=20). 3 tests REGLA-T53.
**Nodo-136 (Tier Detection CTI Fallback — D136-01):** `edge_calculator.py` L932-948: cuando `torneo_nombre=''` (H2H combinado sin metadato), lee CTI de `ranking_analysis.prediction.circuit_asymmetry`. Umbrales: CTI_max<0.6→itf, <1.5→challenger, ≥1.5→atp500. Resultado: 12 picks todos-atp500 → 4 atp500 + 5 itf + 3 challenger. Mega desbloqueado. D136-02 (gap): fix en `extraer_historh2h.py` para propagar `torneo_nombre` — pendiente sesión futura.
**Nodo-137 (Governor MOTOR Exclusion — D137-01):** `combo_governor.py` L359: MOTOR excluido del gate de combos (`all_stakes` solo incluye confianza+rival+betplay). MOTOR muestra como referencia con WARN>40% bankroll. Fix colateral: `rival_value_betslip.py` OSError subprocess sin intérprete Python → `sys.executable` prefix. Impacto: governor pasa de BLOCK permanente → permite combos.
**Nodo-153 (Score Intelligence — Serve/Break/Game Live 2026-07-29):** D153-01: `current_set_home/away` extraídos desde statistics.sets.home/away (marcador split del set en curso, no solo total de juegos). D153-02: `serving` = "home"/"away" desde statistics.sets.homeServe (quién sirve ahora). D153-03: `game_score` = "30:15" desde liveData.score.home/away (marcador del game actual). D153-04: `break_situation` = True cuando no-servidor lidera en set en curso (quiebre confirmado, diferencia>0 con el servidor sirviendo). `_fmt_progreso()` nuevo formato display: `7:6,1:6, ►2:2 [15:30] | 24j` (sets-completados, servidor-indicator ►/◄, set-en-curso, game-score, total-juegos, quiebre opcional). Parser `_parse_kambi_livedata_sets()` llena todos los 4 campos D153 desde livedata endpoint (offering API devuelve liveData.scoreStr=None para ITF LIVE, pero livedata/{id}/.json tiene statistics.sets completo). Caso real verificado: Lazaro vs Mccormick set3 ►3:1 [15:0] home sirve, Vosloo vs Saavedra set3 2:2◄ [15:15] away sirve. 12 tests REGLA-T53 — 12/12 PASS. 2375 tests.
**Nodo-138 (G2 Gate Multi-Signal — D138-01+D138-02):** D138-01: G2 evoluciona de binario n_h2h a 3 reglas: Regla-1=triple convergencia (original), Regla-2=STRONG+edge≥20%+kelly>0+axes≥2, Regla-3=edge≥35%+kelly>0+axes≥2. Reducción: G2 bloquea 2/18 picks (era 11/18). Raíz: double-penalization — MOTOR ya incorpora n_h2h=0 en kelly_kl/shrinkage/p_blend. D138-02: `favoritos_combo_builder.py` torneo='Desconocido' usa `_match_{partido}` como clave única (no colapsar todos en mismo torneo). 9 tests REGLA-T53 — 9/9 PASS.
**Nodo-153 (Score Intelligence — Serve/Break/Game Live 2026-07-29):** D153-01: `current_set_home/away` extraídos desde statistics.sets.home/away (marcador split del set en curso). D153-02: `serving` = "home"/"away" desde statistics.sets.homeServe. D153-03: `game_score` = "30:15" desde liveData.score.home/away. D153-04: `break_situation` = True cuando no-servidor lidera. `_fmt_progreso()` display: `7:6,1:6, ►2:2 [15:30] | 24j QUIEBRE`. Parser `_parse_kambi_livedata_sets()` llena 4 campos D153 desde livedata endpoint (offering API scoreStr=None para ITF). Casos reales 2026-07-29: Lazaro set3 ►3:1 [15:0] (home sirve), Vosloo set3 2:2◄ [15:15] (away sirve). 12 tests REGLA-T53 — 12/12 PASS. 2375 tests.
**Nodo-152 (Phantom History Guard — Circuit Consistency Validation 2026-07-29):** thf_cache asignó historial top-ATP a Vesantera B.T. (ITF M15) por matching apellido sin validación circuito. Pérdida real: AC1–AC12 con edge=39.5% inventado. D152-01: `_validate_circuit_consistency()` función pura 5 reglas. D152-02: hard block en ruta THF. D152-03-D152-05: gates data_quality + ELO-ranking. D152-06: `scripts/audit_phantom_history.py` retroactivo. Relación Nodo-72: homónimos vs contaminación circuito. 8 tests REGLA-T53 — 8/8 PASS. 2371 tests.
**Nodo-150 (Live Games Risk Intelligence — Simulación Doctoral 2026-07-28):** 5 gaps arquitectónicos expuestos por simulación 3 piernas UNDER live (Novkirichka UNDER 20.5 @1.91 + Alexandrova UNDER 32.5 @1.96 + Dahlin UNDER 22.5 @1.98). Gap-1 ENVENENADA_CUOTA: D150-01 — cuota_drift >+15% → `cuota_envenenada=True` + log [ITF_LIVE] CUOTA_ENVENENADA (Alexandrova: cuota 1.96→2.55 = +30.1% no detectado). Gap-2 SET1_TIEBREAK_GATE: D150-02 `games_set1` en `_parse_kambi_livedata_sets` (primer set: h+a, tiebreak=13); D150-03 zona=`COINFLIP_FORZADO` en ambos call sites cuando games_set1≥12. Gap-3 CUOTA_CRASH_CONFIRMACION: D150-05 badge verde MERCADO CONFIRMA en X3 cuando drift<-15% + EN_VIVO/ITF_VIVO (Novkirichka -26%, Dahlin -27%). Gap-4: D150-04 `games_set1=None` parámetro en `_calcular_certeza_condicional` — override interno DOMINANTE→COINFLIP si tiebreak. Gap-5: D150-06 SET1_SCORE_GATE + CUOTA_ENVENENADA gate antes de `_fire_itf_live_games_combo` — piernas con tiebreak o cuota envenenada excluidas con log [ITF_LIVE_GATE]. D150-07 (addendum post-monitoreo): CERTEZA_MATEMATICA override de LINEA_ENVENENADA — `elif _envenenada and not certeza_matematica` en badge chain X3 (elimina paradoja visual [CERTEZA]+[LINEA ENVENENADA]); `alta_itf_raw` gate añade `OR certeza_matematica` (señales 96%+ certeza no excluidas del combo por drift de línea ya absorbido). Log [ITF_LIVE_D150-07] cuando override activa. Caso prototipo: Martin Damm UNDER 30.5 gp=23j linea_drift=+6j certeza=96.2%. H150-01/H150-02/H150-03 pre-registradas (preregistered_hypotheses.json). 13 tests REGLA-T53 — 13/13 PASS. 2356 tests.
**Nodo-151 (Live Edge Gates — Root Cause 1/5 Hit Rate 2026-07-28):** 3 gates corrigen defecto estructural: sistema usaba p_model (Modelo A pre-partido) para edge live mientras p_condicional (Modelo B Gaussiano) mostraba edge NEGATIVO en las 5 señales — sin poder de veto. Caso confirmado en vivo: Dart vs Cross OVER 20.5 ALTA en dashboard, score real 6:2 5:1 = UNDER certeza matemática (score_str=null = sistema sin marcador que bookmaker sí tenía). D151-01: `_edge_live_gate(certeza, cuota_live, umbral=0.05)` — edge_live=p_condicional-(1/cuota_live)<5% → excluir. D151-02: `_score_null_gate(score_data, gp_min=3)` — score_str=null + games_played>3 → excluir (desventaja informacional). D151-03: `_zona_direccion_gate(zona, dir, linea, certeza, p_umbral=0.40)` — zona contradice dirección + p_cond<40% → excluir (Zarazua OVER@19.5 con DOMINANTE µ=18). 3 funciones puras en `live_desk.py` antes de `_fire_itf_live_games_combo`. Gates integrados en loop `alta_itf` después de D150-06b. H151-01 pre-registrada (n_stop=20, hit rate ≥40% objetivo). 10 tests REGLA-T53 — 10/10 PASS. 2363 tests.
**Nodo-147 (Live Score × Games Convergencia — Certeza Condicional en Tiempo Real):** 4 gaps identificados entre señal y realidad operativa: (1) `_parse_kambi_tennis_score()` existe pero nunca conectado al panel X3; (2) baseline pre-partido se pierde cuando evento entra EN_VIVO (solo ITF_VIVO tenía D142-T0 freeze); (3) p_modelo estático sin actualización condicional P(UNDER | games_played=k); (4) sin historial de cuotas games para sparkline de tendencia. D147-01: `_enrich_live_score(signals, live_events)` — mutación in-place con score_data desde `_parse_kambi_tennis_score()`, índice O(1) por event_id. D147-02: `_calcular_certeza_condicional(linea, direccion, games_played, sets_complete, current_set_home, current_set_away, zona)` — certeza_matematica (peor_caso = sets_remaining*13), p_condicional Gaussiano (DOMINANTE µ=18 σ=3, COINFLIP µ=23 σ=4.5), alerta_nivel CERTEZA/ALTA/MOD/BAJA. D147-03: `_freeze_baseline_if_needed()` → `reports/games_baseline_{fecha_compact}.json` inmutable (primer ciclo EN_VIVO). D147-04: panel X3 nuevas columnas Base(T0)/Live/Drift/Progreso/Certeza con badges HTML coloreados + fila verde si certeza_matematica. D147-05: `_write_games_odds_history()` append-only sparkline → `reports/games_odds_history_{fecha_compact}.json`. D147-06: banner blink CERTEZA_MATEMATICA + Telegram alert fire-once (`reports/certeza_fired_{fecha_compact}.json`). H147-01 pre-registrada (DOMINANTE p_condicional≥0.70 games_played>linea/2, n_stop=20). 6 tests REGLA-T53. SPEC — pendiente implementación.
**Nodo-146 (H2H_MODEL Universe: Bridge REGLA-HF-1 Gap en FAVORITOS_COMPUESTOS):** Gap arquitectónico: Vancouver 23 partidos todos @1.10–1.49 → REGLA-HF-1 los descarta en edge_calculator → 0 picks Vancouver en edge_report → favoritos_combo_builder sin piernas. REGLA-HF-1 correcta para singles pero NO bloquea combos (4 fav @1.25 = @2.44x). D146-01: `_find_latest_h2h()` + `_leer_h2h_favoritos()` en `favoritos_combo_builder.py` leen `h2h_results_enhanced_*.json` directamente — universo H2H_MODEL independiente de REGLA-HF-1. MAX_H2H_MODEL_PER_COMBO=2 (máx 2 piernas H2H_MODEL por combo para no diluir). Timing guard D145-02 integrado. Automático sin flag. 9 tests REGLA-T53 — 9/9 PASS. 2324 tests.
**Nodo-145 (Pipeline Bugs tipo_cancha+timing):** Root cause cascada 0 combos 2026-07-27: `tipo_cancha='N/A'` → bucket `unknown` (24% hit rate) → Kelly aplastado → pipeline_picks vacío → 0 CORE/Confianza. D145-01a: `h2h_extractor.py` (Playwright) `tipo_cancha` usa `superficie` como fallback + copia `hora`. D145-01b: `ninja_h2h_parser.py` (--api-mode) mismo fix en rama `list` de `load_matches()` + `_consolidate_result()` + hora propagada. D145-02: `edge_calculator.py` timing guard — skip partidos con hora > 15min en el pasado (Boitan G.A. @2.38 ya había perdido cuando pipeline corrió). 7 tests REGLA-T53 — 7/7 PASS. 2314 tests.
**Nodo-144 (Trazabilidad de Estrategia en Shadow Book — D144-01→D144-07):** Brecha E3 confirmada: 24 JSONL sin tag de estrategia. D144-01: `_build_record()` añade `strategy` top-level con default `'SIN_TAG'` (inmutable: nunca toca `pick_snapshot`). D144-02: `tag_strategy(fecha, player_names, strategy)` — update top-level sin tocar snapshot (patrón `update_alpha_flags`). D144-03: `combo_confianza_builder.py` llama `tag_strategy()` tras construir el plan para CORE/COBERTURA/SATELITE/MOONSHOT/ANCHOR/GCS. D144-04: `log_pick(fecha, jugador, cuota, pick_snapshot)` — registra pick individual; fix `favoritos_combo_builder._registrar_shadow_book()` que llamaba función inexistente silenciosamente. D144-05: `report()` añade segmento ESTRATEGIA agrupando settled por strategy (excluye SIN_TAG/HISTORICO). D144-06: `scripts/backfill_strategy.py` — cruce combo_registry (3 días: 22/23/25-jul, alta confianza nombre+fecha+subtipo) → 22 registros tageados; restantes 1287 → HISTORICO_SIN_TAG. D144-07: 7 tests REGLA-T53. Valores válidos: MOTOR/CORE/SATELITE/COBERTURA/MOONSHOT/ANCHOR/GCS/SAFE/WAS/MEGA/GAMES/RIVAL_VALUE/FAVORITOS_COMPUESTOS/SIN_TAG/HISTORICO_SIN_TAG. Deuda: D144-08 extender a MOTOR/SAFE/WAS/MEGA/GAMES. 7 tests — 7/7 PASS. 2307 tests.
**Nodo-143 (Match Ledger Torneo Metadata Propagation — D143-01):** Root cause de torneo="Desconocido" en todos los picks Kambi. `fusionar_dia()` en `scraping/match_ledger.py` usaba FlashScore como base del join y descartaba silenciosamente `tier|torneo_nombre|torneo_completo|pais|ranking1|ranking2|tournament_context` de Kambi. Evidencia 2026-07-25 mañana: 40 joins → 0/40 con torneo. Fix: 7 líneas post-`partido_merged`, guard fill-gaps `not partido_merged.get(campo)`. Cadena completa verificada: ledger→h2h_results_enhanced→detectar_tier()→lambda_efectivo→Kelly-KL. NOTA: el fix requiere re-correr PASO 2 (extraer_historh2h) después del ledger — el h2h lleva torneo_nombre del merged file. Evidencia producción tarde 2026-07-25: 17/17 joins con torneo, Suresh D. tier=challenger/torneo=Bloomfield Hills (antes atp500/Desconocido), 5 combos $15k generados (CORE @16.44x $7,000 — antes 0 combos). Gap D143-04: combo_confianza_builder.py L658 gate log usa `torneo` no `torneo_nombre` → muestra '?' (cosmético). Nodo-142 HUÉRFANO sin spec — D143-03 pendiente. 5 tests REGLA-T53 — 5/5 PASS. Regresión Nodo-118: 23/23 PASS.
**Nodo-141 (Kambi-Only Edge Report — D141-01→D141-03):** Solución definitiva al problema de 0 combos apostables cuando los picks son mayoritariamente ITF. D141-01: `scripts/filter_kambi_picks.py` — lee `edge_report_FECHA.json` y produce `edge_report_kambi_FECHA.json` con SOLO picks `kambi_disponible=True`. D141-02: `run_daily.py` PASO 3K — corre `filter_kambi_picks.py` inmediatamente después de PASO 3 (edge_calculator). `optional=True` si sin coverage. D141-03: `betplay_combo_builder._find_latest_edge_report()` — prefiere `edge_report_kambi_HOY*.json` (picks 100% apostables en Betplay); fallback a full report si no existe kambi de hoy. `combo_confianza_builder` ya elige el kambi report automáticamente por mtime más reciente (sin modificar). Hallazgo: PASO 1 ya es `extraer_partidos_api.py` (Kambi primario); el problema era que PASO 1b Playwright (+228 ITF) inundaba el edge_report. PASO 3K es el filtro correcto: no toca la colección de datos, solo el reporting para combos. 8 tests REGLA-T53 — 8/8 PASS.
**Nodo-140 (Kambi Gate — D140-01→D140-04):** 3 cirugías para cerrar la desconexión entre `fetch_kambi_coverage.py` (Nodo-90 D90-01, existía pero nunca corría) y los combo builders. D140-01: `run_daily.py` PASO 1c — `fetch_kambi_coverage.py` corre diario ANTES de edge_calculator → `kambi_disponible` fresco en cada pick. D140-02/03: `_filter_kambi_available()` en `betplay_combo_builder.py` — pre-filtro en `build_safe_combos()` (edge_pick_map), `build_was_combos()` (watchlist), `build_mega_combos()` (edge_tier_map + pool). D140-04: gate `is_player_available()` en `combo_confianza_builder.py::_extract_and_categorize()` — excluye favoritos ITF/torneos sin Betplay antes de categorizar. `None` = pass-through (sin coverage). Hallazgo clave: campo `kambi_disponible` existía en edge_report desde Nodo-90 — nunca fue consumido. 9 tests REGLA-T53 — 9/9 PASS. Deuda catalogada: D141-01 unificar 5 normalizadores de nombres en silos.
**Nodo-139 (Kambi-First Combo Builder — D139-01→D139-07):** Inversión arquitectónica: en vez de buscar picks en Kambi post-hoc, parte del universo Kambi NOT_STARTED cuota [1.10,1.80] (136 events). D139-01: `_fetch_kambi_betting_universe()` — HTTP listView, filtra cuota, convierte UTC→Colombia HH:MM. D139-02: `_match_to_predictions()` — `_apellido_kambi()` (último token) + `_apellido_pick()` (quita iniciales ≤2 chars, toma primer token) + bigram Jaccard ≥0.70 → TIER_A (apostar/watchlist), TIER_B (sin_edge + p_modelo>p_implied). D139-03: gates G_EDGE (edge>0) + G_CONF (p≥0.55). D139-04: `_build_kambi_combos_kf()` — SIN cap de cuota (COMBO_MAX_CUOTA=7.0 era matemáticamente inválido: EV>0 cuando ∀leg edge>0 independiente del producto). EV gate EV_MIN_COMBO=2% + MIN_P_COMBO por n_legs. D139-05: half-Kelly con portfolio factor RHO=0.15, cap [500, 3% bankroll]. D139-06: KB_N.bat en Desktop. D139-07: `--kambi-first` flag en `betplay_combo_builder.py`. Fix colateral: `_find_outcome()` en `combo_confianza_builder.py` — si último token ≤2 chars (inicial), usa primer token como apellido (bug McFadzean L.→'l'). H139-01/02 pre-registradas. 15 tests REGLA-T53 — 15/15 PASS.
**Nodo-135 (EvalGames Live API Fix):** D135-01: `_extract_games_cuota_live(event_id, direccion, linea)` — HTTP a `betoffer/event/{id}.json` via `urllib.request` (no listView que devuelve `[]` para ITF). D135-02: filtro `" - Set "` y `"Juego"` excluye sub-mercados set/juego; solo acepta match-total "Total de juegos". D135-03: call-site `_check_games_convergencia()` actualizado a `matched["event"]["id"]`. Evidencia real 2026-07-21: listView 225 STARTED → 43 con Total de juegos (todos ATP/WTA/Challenger), 0 ITF. betoffer/event devolvió cuotas reales: Brockmann/Jacquemot OVER @ **1.55**, Suresh UNDER @ **1.65** (drift −6.8%). 4 tests REGLA-T53 — 4/4 PASS.
**Nodo-133 (Games Live Convergencia):** D133-01: `_kambi_started_events()` — 1 HTTP call a Kambi listView STARTED + liveEvents.json, sin Playwright. D133-02: match por `event_id` (primario) + apellido normalizado (fallback). D133-03: `_check_games_convergencia()` llamada en `_background_refresh()` cada 15s — clasifica señales ALTA como EN_VIVO/PRE_PARTIDO/TERMINADO. D133-04: si ≥2 ALTA EN_VIVO → anti-flood `_fired_games_{fecha}.json` (cap 10/día) → `subprocess.Popen(betplay_combo_builder --games --live)` fire-and-forget. D133-05: escribe `reports/games_live_YYYYMMDD.json`; `_build_x3_games()` enriquece señales con `estado_live`, `cuota_live`, `drift_pct`. D133-06: umbral ≥2 ALTA EN_VIVO. Banner CONVERGENCIA ACTIVA parpadeante + columna Estado en X3 panel. Sin cambios en live_edge_monitor, close_snapshot_server ni betplay_combo_builder. 5 tests REGLA-T53 — 5/5 PASS.
**Nodo-129 (LiveDesk AutoRefresh — 3 capas):** D129-01: `_STATE_CACHE` en memoria TTL 20s + thread daemon `_background_refresh()` cada 15s — primera request =60s, todas las demás =<1s. D129-02: `DeskHandler.do_POST()` en `live_desk.py` + `_notify_live_desk()` en `close_snapshot_server.py` tras `/check-and-close` y `/live-check` — n8n invalida cache explícitamente via POST a `:7780/api/refresh`. D129-03: `_data_freshness()` mtime real de edge_report/live_odds_history; header muestra "datos de hace 2m 15s" en lugar de timestamp de request. JS interval reducido 30s→12s. Latencia máxima real: 90s → ~12s. 3 tests REGLA-T53.
**Nodo-128 (Wplay P8 Multi-Book Alias):** D128-01: alias apellido en TODOS los feeds de `_build_p8_books()` (`"Botic Van De Zandschulp"` → alias `"van de zandschulp"`) para matchear picks con nombres abreviados. D128-02: games_signal_report players injected en P8 (strip inicial trailing `"B."`, seen_oids guard). Resultado: P8 pasa de 0 a **35 picks** con betplay+rushbet+wplay activos. Wplay SSR independiente de Kambi 429. 3 tests REGLA-T53.
**Nodo-127 (GamesSignal ITF OutcomeID Fix):** CERRADO 2026-07-21 (`582090c`). D126-04: `procesar_partidos()` Intento2 ahora filtra `state!="NOT_STARTED"` + usa `_apellido()` + excluye dobles. Intento3 desactivado (un solo apellido matcheaba partidos aleatorios). D126-05: `seen_outcome_ids: dict[int,str]` descarta IDs genéricos reutilizados en 2+ partidos (ID `4265916952` aparecía en 30+ matches ITF). Resultado: GAMES combos solo ATP250+ con IDs únicos. Primer combo válido: GamesA @2.64x (Van De Zandschulp UNDER 25.5 + Oliynykova OVER 19.5). 3 tests REGLA-T53 — 6/6 PASS (N126+N127).
**Nodo-126 (GamesSignal 3 Bugs Fix):** 3 bugs en `games_signal_calculator.py::_buscar_event_id_kambi()` detectados 2026-07-21. D126-01: `split()[-1]` extraía inicial en vez de apellido → nueva `_apellido()` toma último token no-inicial. D126-02: sin filtro dobles → `if "/" in name: continue`. D126-03: `o_mas["odds"]` KeyError → `.get("odds",0)`. 3 tests REGLA-T53. D126-04 pendiente: filtro `state=="NOT_STARTED"` para excluir odds live.
**Nodo-123 (Auditoría Dashboard Integraciones v2):** Auditoría 2026-07-20 — todos los 6 pendientes del spec eran falsos. D122-01→D122-04: `live_desk.py` ya tenía `_fetch_all_odds()` live (L1866), badges STEAM/ATN (L927-991), mensaje X3 accionable (L1051-1054), systemd service habilitado. D122-05: `_leer_matches_ranking_only()` ya existe vía `--matches`. D122-06: `KambiLiveClientReal` (`live_edge_monitor.py` L158) operativo desde D97-15 2026-07-14 — Fable encontró endpoint público `kambicdn.com` sin DevTools. Lección: auditar código real antes de spec.

---

## 6. MAPA DE ARCHIVOS CLAVE

```
── PIPELINE ─────────────────────────────────────────────────────────────────
extraer_URL_partidos_version2.py  ← PASO 1 PRIMARIO (Playwright entity IDs FlashScore)
extraer_partidos_api.py           ← PASO 1 FALLBACK (API — vulnerable a homónimos)
extraer_historh2h.py              ← PASO 2 (Playwright sin flags | --api-mode fallback)
edge_calculator.py                ← PASO 3: Kelly-KL 5 capas
generar_tabla_favoritos2.py       ← PASO 3.5: revisión humana
trader_ev_tenis.py                ← PASO 4: Hedge Fund Layer + CPPI
combo_confianza_builder.py        ← PASO 4.3: CORE/Satellite/Moonshot
betplay_combo_builder.py          ← PASO 4.4-4.57: links Betplay
rival_value_betslip.py            ← D68-07: micro-Kelly rival H88-01 (n=3/30, shrink=5.7%)
betslip_registrar.py              ← PASO 4.6: registro + loop calibración
run_daily.py                      ← Orquestador PASO 0→4.3 + settle

── SHADOW BOOK + OBSERVABILIDAD ─────────────────────────────────────────────
shadow_book.py                    ← CLV: log_picks | close_snapshot | settle | report
pipeline_tracker.py               ← READ-ONLY (--section shadow|confianza|drift|portfolio)
pre_game_validator.py             ← cron 0 9-23: kelly_kl=0.0 BLOCK | n<8 WARN
close_snapshot_server.py          ← HTTP :8765 bridge (Nodo-73) — timing exacto por partido
close_snapshot_trigger.py         ← cron */10 9-23h venv/bin/python3 FALLBACK (si n8n cae) — fix 2026-07-10
check_contradictions.py           ← cron lun 9am: CLAUDE.md vs nodos (Vacío 3)

── n8n AUTOMATION (Nodo-73, systemd) ─────────────────────────────────────────
n8n Docker :5678                  ← Tennis Close-Snapshot Timing workflow
tennis-snapshot-bridge.service    ← systemd, enabled, PID en logs/snapshot_bridge.log
n8n_push_workflow.py              ← sube/actualiza workflow via API REST

── DATOS CRÍTICOS ───────────────────────────────────────────────────────────
data/calibracion_edge.json              ← Thompson Beta priors (fuente de verdad)
reports/shadow_book/sb_YYYY-MM-DD.jsonl ← append-only, inmutable en predicción
validation/preregistered_hypotheses.json ← H52-01→H121-01 (19 hipótesis), NO modificar sin decisión
validation/hypothesis_tracker.py        ← sprt_verdict() + llr_update() (Nodo-64)
docs/DECISION-LOG.md                    ← D-01→D-10 + E-01→E-05 + C-01→C-07

── MOTOR DE PREDICCIÓN ──────────────────────────────────────────────────────
analysis/rivalry_analyzer.py      ← Erdős+Markov+GCS+PhantomGuard (núcleo)
analysis/markov_analyzer.py       ← PELT + surface_context_discount
config.py                         ← detectar_tier() — fuente única de tiers
core/data_contract.py             ← PICK_STATUS_NO_DATA + DataContract v2: validate_artifact() 6 fronteras (C1 Nodo-67)
core/player_registry.py           ← entity resolution canónica (Nodo-51)

── INSTRUMENTOS FASE 4 (REPORTE_SOLO, no cambian decisiones) ────────────────
analysis/drift_monitor.py | flb_curve.py | pattern_audit.py
analysis/conformal_band.py | rho_empirical.py | velocity_monitor.py

── ML (SUSPENDIDO hasta modelo > 78% held-out) ──────────────────────────────
generar_dataset_plus.py | aplicar_enhancer.py

── SUSPENDIDO (isla Flask) ───────────────────────────────────────────────────
app.py | routes/ | models/ | services/ | database.db
```

---

## 7. BUGS ACTIVOS

| Bug | Estado |
|---|---|
| Auditoría Nodo-86 (15 hallazgos) | ✅ 12 fixes D87-01→D87-11 + D64-01 aplicados 2026-07-11 — 18/18 tests REGLA-T53 verificados 2026-07-12 en WSL. Gap D87-07/D87-10: embebidos en `trader_ev_tenis.main()`, requieren refactor para aislar — candidato sesión futura |
| prediccion_ganador top-level=None | ✅ RESUELTO — usar `ranking_analysis.prediction.favored_player` |
| Edge falso historial corto (n<8) | ✅ RESUELTO — Nodo-63 `_MIN_HISTORY_FOR_DECAY=8` |
| Phantom Identity API homónimos | ✅ RESUELTO — Nodo-72 `_detect_phantom_identity()` + Playwright PRIMARIO |

---

## 8. PROTOCOLO DE TRABAJO

```bash
# 1. Buscar en git antes de implementar (GIT-FIRST — obligatorio)
git log --all --oneline -- '*keyword*'
git show COMMIT:backend/archivo.py    # recuperar si existe

# 2. Baseline antes de modificar
python -m pytest tests/ --no-cov -q  # 1986 passed

# 3. Syntax check después de editar
python -c "import ast; ast.parse(open('archivo.py').read()); print('OK')"

# 4. Graphify antes de grep (grafo existe)
graphify query "<pregunta>"   # orientarse primero, grep solo para líneas específicas
```

**SDD:** Ningún código sin Nodo en `.spec/01_Nodos/`. Ver `PRE_IMPLEMENTATION_CHECKLIST.md`.
**URLs:** `ninja` API ≠ `flashscore.com` DOM. NUNCA derivar URLs browser desde URLs API.

---

## 9. RECORDATORIOS CRÍTICOS

**Guards No-Ruina:** HF-1 (cuota<1.50 nunca en pool) | HF-5 (KGR<0 → NO DESPLEGAR) | VaR auto-ajustado (no calcular a mano) | `--torneo-tipo` filtra por tier — NO mezclar GS con ITF.

**Calibración:** p_prior automático (tier+superficie, `calibracion_edge.json`). confidence: STRONG≥0.60 | MOD 0.55-0.60 | LOW<0.55. Shrinkage n/(n+20): n<10 = revisar antes de apostar.

**Datos:** Predicción anidada en `ranking_analysis.prediction.favored_player`. Phantom alerta: ranking=None + n>20 + oldest>365d → LOG_PLAYWRIGHT_CANDIDATE. `_MIN_HISTORY_FOR_DECAY=8`.

**Testing:** REGLA-T53 (función real, nunca hardcodear fórmula). 2204 tests: no romper.

**Combo Builder:** correr trader POR TIER antes del combo builder. REGLA-KAMBI-1: `||replace` (no `||append`).

**REGLA-BAT-1 (Betplay URL format — INMUTABLE):** Formato coupon: `combination|ID1,ID2,ID3||replace` (IDs separados por comas, SIN sufijo `|ML/`). Pipeline: `.bat → file:///Desktop/combos/combo.html → JS window.location.replace(url) → Betplay`. NUNCA poner la URL de Betplay directo en el .bat (Chrome encodea `|` → `%7C` → coupon roto). NUNCA usar `|ML/` ni GitHub Pages redirect. Fuente de verdad: `combo_confianza_builder.py::_generar_bats()` L1675.

**Tamp (proxy :7778):** dependencia dura — si Claude Code no responde, ver `TROUBLESHOOTING.md`. Arreglo rapido: `systemctl --user restart tamp`.

---

## 10. POLÍTICA DE PRECEDENCIA (§1.2 Vacío 3, FABLE_02)

1. `.spec/01_Nodos/` es **historia inmutable** — no editar; añadir nueva entrada o marcar `SUPERSEDED por [[Nodo-XX]]`.
2. **CLAUDE.md es VISTA derivada** — si contradice al nodo más reciente, CLAUDE.md está desactualizado.
3. `python3 check_contradictions.py` (cron lunes 9am) compara CLAUDE.md vs últimos 10 nodos.

---

## 11. TAXONOMÍA DE ESTRATEGIAS — LAS 13 FORMAS DE GENERAR COMBOS

> Spec completo: [[Nodo-100-Taxonomia-Estrategias-Generacion-Combos]]
> Todas leen `edge_report_*.json` como fuente única. Diferencia: qué hacen con la señal.

### Generadores y sus estrategias

```
edge_calculator.py  (PASO 3 — corre siempre, fuente única)
   │
   ├─ trader_ev_tenis.py          → (1) EL MOTOR
   ├─ combo_confianza_builder.py  → (2) CORE  (3) SATELITE  (4) MOONSHOT
   │                                 (5) COBERTURA  (6) ANCHOR  (7) GCS
   ├─ betplay_combo_builder.py    → (8) SAFE  (9) WAS  (10) MEGA  (11) GAMES  (12) ANCLA SEGURA
   └─ rival_value_betslip.py      → (13) RIVAL VALUE
```

| # | Nombre | Flag/Comando | Piernas | Cuota | Stake | Condición clave |
|---|--------|-------------|---------|-------|-------|-----------------|
| 1 | **EL MOTOR** | `trader_ev_tenis.py` | 1 | variable | Kelly-KL | edge>5% + KGR>0 + todos los gates |
| 2 | **CORE** | `combo_confianza_builder.py` | 4–7 | @2–5x | $2k–5k | Cat-A/B, P(win)≥25% |
| 3 | **SATELITE** | `--fase 2+` | 5 | @5–8x | $2k–3k | Cat-C1 disponible (conf≥60%) |
| 4 | **MOONSHOT** | `--fase 3+` | 5 | @15–35x | $1k–2k | ≥2 picks Cat-C conf≥57% |
| 5 | **COBERTURA** | `--fase 4` | 4–7 | @2–4x | $1k–2k | Fase 4 — hedge del CORE |
| 6 | **ANCHOR** | `--anchor` | 4–5 | @4–35x | $1.5k | prioridad≥75 + cuota≥1.65 |
| 7 | **GCS** | automático en hierba | 2–3 | @1.5–3x | $200–500 | gcs_active + tier≥ATP500 |
| 8 | **SAFE** | `betplay_combo_builder.py --safe` | 2 | @3–12x | $1k | P(ambos)≥25%, torneos distintos |
| 9 | **WAS** | `--live` (incluido) | 2–3 | @4–25x | $5k | edge≥10% + señal Markov explícita |
| 10 | **MEGA** | `--mega` | 6–10 | @100–1000x | $500 | Dispersión DIFF (std≥0.04) |
| 11 | **GAMES** | `--games` | 1 | @1.8–2.1x | $1k–2k | mercado Over/Under en Kambi |
| 12 | **ANCLA SEGURA** | `--ancla-segura` | 4 | variable | $3k | ancla STRONG cuota≥2.50 + fillers STRONG/MODERATE only (LOW excluido) |
| 13 | **RIVAL VALUE** | `rival_value_betslip.py` | 1 | @2.5–8x | $2k | edge_fav≤−10% (H88-01) |

### Hit rates reales — shadow book 2026-07-01→14 (picks individuales, n=231 settled)

| Segmento | Hit% | ROI flat | IC 95% | Estado |
|----------|------|----------|--------|--------|
| **GCS** (H60-01) | **64.8%** | — | — | GRADUADA n=54 — MEJOR ESTRATEGIA |
| **RIVAL VALUE** (H88-01) | **100%** | +275% | [52.6,100] | n=3 — pre-graduación, no significativo |
| **VARIABLE** (edge≤0) | **62.5%** | −24.7% | [42.7,78.8] | n=24 — pre-graduación (ROI neg por cuotas bajas) |
| **Grand Slam** | **47.4%** | **+40.2%** | [27.3,68.3] | n=19 — mejor ROI real |
| **GS+watchlist+edge≥20%** | 37.5% | **+53.8%** | [13.7,69.4] | n=8 — señal fuerte pero n pequeño |
| **season_transition** | 42.2% | +11.9% | [29.0,56.7] | n=45 |
| **ANCHOR** (edge>0) | 34.3% | −5.7% | [28.2,41.0] | n=207 |
| **Challenger** | 37.2% | −5.5% | [27.3,48.3] | n=78 |
| **WATCHLIST** | 34.9% | −5.2% | [27.7,42.8] | n=149 |
| **ITF** | 36.9% | −13.5% | [28.5,46.2] | n=111 |
| **EL MOTOR** (APROBADO) | 30.0% | −21.1% | [14.5,51.9] | n=20 — gates muy estrictos = picks de alta cuota |
| **WAS** (H52-01) | — | — | [18.6,49.9] | NO GRADUABLE |

**Lectura clave:** El ROI negativo en picks individuales NO indica que los combos fallen — los combos multiplican cuotas. Un pick CORE @1.40 con 34% hit rate en flat-1u puede ser correcto en kelly. La estrategia GCS es la única GRADUADA con hit% formal ≥breakeven.

**Hallazgo 2026-07-14:** RIVAL VALUE = discriminador `edge_fav ≤ −15%` suficiente aunque señales secundarias contradigan. Es la estrategia con mayor potencial de ROI (41.25x combinada hoy).

---

## graphify

Grafo de código en `graphify-out/` (949 nodos, 1302 edges + 91 nodos .spec/ — código Python + memoria semántica, reindexado 2026-07-13).

- **Visualización 2D:** http://localhost:7779/graph.html (Nodo-83 — vis.js, F5 = datos frescos)
- **Visualización 3D:** http://localhost:7779/graph3d.html (Nodo-84 — Three.js/ForceGraph3D, rotación orbital)
- **Verificar:** `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:7779/graph.html` → debe retornar `200`
- Antes de grep: `graphify query "<pregunta>"` | `graphify path "<A>" "<B>"` | `graphify explain "<concepto>"`
- Actualizar tras cambios: `graphify update .` → F5 en browser muestra nuevos nodos sin regenerar HTML
- Para incluir `.spec/` docs: `export ANTHROPIC_API_KEY="sk-ant-..." && graphify .`
- Gestión servicio: `systemctl --user start|stop|restart graphify`
